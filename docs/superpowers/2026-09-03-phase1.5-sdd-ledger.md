# SDD ledger — plan: docs/superpowers/plans/2026-09-03-phase1.5-models-redesign.md
Spec: docs/superpowers/specs/2026-09-02-babelay-design.md
Branch: main (user directive from phase 1 stands; no worktree)
Shell note: mise not activated → `mise exec -- yarn …`

## Pre-flight scan
| Pair / Task | Produces vs consumes | Finding |
|---|---|---|
| T1↔T2 | lib.rs declares `pub mod download` in T1 with an empty file; T2 fills it | consistent (T1 must create the stub) |
| T1↔T3 | REGISTRY/BALANCED/find/installed/model_path; ModelInfo: Serialize+Clone | consistent; T3 clones ModelInfo into ModelStatus |
| T2↔T3 | download(client, url, dest, expected_size, sha256, cancel, on_progress) | consistent; T3 adds reqwest dep to src-tauri for Client |
| T3↔T5 | commands get_models/download_model/cancel_download/delete_model, event model-download {id, received, total, state, message} | TS types in T4 match |
| T3↔T6 | overlay_get_monitors removed in T3; frontend call removed in T5 (tauri.ts) and T6 (page) | between T3 and T5 the frontend still references overlayGetMonitors → T3 skips `yarn build`; T4's gate runs `yarn build` — Overlay.tsx still calls api.overlayGetMonitors (exists in tauri.ts until T5) so build passes; runtime would fail only on the overlay settings page. Acceptable window. |
| T4↔T5 | minimal useModels in T4 replaced by full store in T5; ModelStatus types added in T4 | consistent |
| T4↔T7 | PillButton `ghost` variant added in T4, used in T5/T6/T7 | consistent |
| T5↔T6 | `overlay.adjust`/`adjustDone` locale keys introduced in T6; T5 doesn't use them | consistent |
| T5↔T7 | ModelRow(status, selected, onSelect); useModels download/refresh; models.continue/continueWith keys added in T5 | consistent |
| T6 self | SettingRow `as` prop introduced mid-task (Task 4 defines SettingRow as <label>) | T6 must add `as?: "label" \| "div"` to SettingGroup.tsx — noted in dispatch |
| T7 self | ErrorBar props unknown; plan says adapt | ok |
| T1 self | Qwen 3.5 GGUF URL uncertain; NEEDS_CONTEXT rule | ok |
| T3 self | `spawn_blocking` closure captures &'static ModelInfo and AppHandle | ok |

Ruling: T3 may skip `yarn build` (frontend still references overlay_get_monitors until T5) — cost if wrong: none, only a temporary runtime gap on the overlay settings page between T3 and T5.

## Tasks
Task 1: minor (deferred): installed() exact-size ceiling (add ponytail comment); reqwest/sha2/thiserror unused until T2; speed ties hand-assigned
Task 1: complete (commits eadf7bb..78436b2, review clean)
Task 2: minor (deferred): Content-Range unvalidated on 206 (sha256 is the gate); total can be 0/overshoot with corrupt .part (UI must clamp); Mismatch carries only path
Task 2: Ruling: fold two one-liners into Task 3 — `resuming = 206 && have > 0` in download.rs, and `read_timeout(30s)` on the blocking client so a stalled socket becomes cancellable — cost if wrong: a slow-but-alive mirror that stalls >30s between bytes fails with Http error and needs a retry.
Task 2: complete (commits 78436b2..f0ae353, review clean)
Task 3: review (c6f2f42) — 1 Important: active slot leaks if the download thread panics. Fix round 1 dispatched (RAII guard + poison-safe locks + Instant checked_sub).
Task 3: minor (deferred): delete() TOCTOU vs concurrent start (unreachable by UI); list() holds active across fs::metadata; solicited-206 Content-Range unvalidated; default models are in_use before install so delete always errors for them — T5 ModelRow already disables delete when in_use
Task 3: fix round 1/5 (3 addressed, 0 open; commits c6f2f42..c1464be)
Task 3: minor (deferred): panic path emits no terminal event (UI stalls until next get_models); tiny window where a done-triggered download returns "busy"
Task 3: complete (commits f0ae353..c1464be, review clean)
Task 4: review (7f0cd15) — 3 Important: SettingRow <label> binds first button; collapsed nav items lack aria-label; divide-surface invisible in light theme. Fix round 1 dispatched (+ px-0 and ProgressBar mt-2 cleanups).
Task 4: minor (deferred): hardcoded sidebar shadow rgba; SegmentedControl tab roles without arrow keys (radiogroup would be more honest); status.idle/capturing locale keys orphaned (dot has no text alternative); footer empty before settings load
Task 4: fix round 1/5 (5 addressed, 0 open; commits 7f0cd15..0e1b27a)
Task 4: minor (deferred): Tailwind content scan picks up docs/design/mockups (and possibly .superpowers) → dead classes in bundle; fix by `@source not "../docs";` in index.css — fold into Task 6
Task 4: complete (commits c1464be..0e1b27a, review clean)
Task 5: review (793614e) — spec ❌ text-accent link; 2 Important: keyboard Enter on inner button double-fires; in_use precedence blocks downloading the default model. Fix round 1 dispatched. Ruling: rowAction order cancel → download(!installed) → delete(in_use) → select; in_use badge only when installed — backend in_use semantics unchanged (delete still refused for selected models) — cost if wrong: a selected-but-missing model shows no badge.
Task 5: minor (deferred): div[role=button] nesting a <button> (axe nested-interactive); nested <label> in Models GPU row (use as="div"); ModelRow subscribes to whole store; error event with null message is silent; orphaned keys overlay.monitor/primary (T6), translation.backend
Task 5: fix round 1/5 (3 addressed, 0 open; commits 793614e..9428734)
Task 5: Ruling: delete is unreachable in the UI (in_use rows can't delete; installed-not-in-use rows show only 선택). Fold into Task 6: installed && !in_use rows render "선택" + ghost "삭제"; in_use rows render no button (badge suffices); rowAction keeps "delete" only for the in_use case but ModelRow renders nothing for it — cost if wrong: one extra button per installed row.
Task 5: complete (commits 0e1b27a..9428734, review clean)
Task 6: minor (deferred): Overlay.tsx toggleAdjust passes String(e) to setError (pass e); general.shortcuts orphan key; accent-[#1ed760] literal vs token; SegmentedControl track invisible inside SettingGroup (same bg); nested buttons in role=button row — fold key cleanup + setError(e) into Task 7
Task 6: complete (commits 9428734..be46453, review clean)
Task 7: review (61057c7) — 1 Important: stale `waiting` auto-advances past asr with an uninstalled model. Fix round 1 dispatched (guard on chosenId, clear on back, double-click guard).
Task 7: minor (deferred): next() non-functional setIdx; useModels() without selector re-renders per tick
Task 7: fix round 1/5 (2 addressed, 0 open; commits 61057c7..466d254)
Task 7: minor (deferred): footer "받고 계속" stays disabled after a cancelled/failed download until back/next (row download button still works) — candidate for the final fix wave
Task 7: complete (commits be46453..466d254, review clean)
Final review: dispatched (opus) over eadf7bb..466d254
Final review (opus, eadf7bb..466d254): 0 Critical, 5 Important (#1 stale in_use after select; #2 oversized .part → 416 dead end; #3 models in Windows roaming AppData; #4 raw error codes shown + "busy" reachable; #5 onboarding footer latches disabled after cancel/fail), minors 6–14. Verdict: With fixes.
Ruling: fix wave = #1–#5 + #6 (.take() before emit) + #9 (as="div") + #14 (in_use asserts) + spec amendments (§5.3 local data dir, §5.5 button rule). Others stay deferred as ledgered.
Final fix wave: commits 466d254..0e402bf (4): engine 416/oversized .part restart, local data dir + take() + in_use tests, UI refresh-after-select + localized error codes + waiting reset, spec amendments. Gates: cargo 30, vitest 21, clippy/fmt/tsc/build clean. Scoped re-review dispatched (sonnet).
Final fix wave re-review: all 9 findings + spec amendments ADDRESSED; gates green. Three Minor introduced:
Parked (final): ClearActive can take a successor's slot in the µs window after the terminal emit — Ruling: unreachable across an IPC round trip; give ClearActive the id in phase 2 when engine writers share the slot.
Parked (final): stale lastEvent un-disables the onboarding footer on retry → double-click can hit backend "busy" (now localized) — Ruling: cosmetic; clear lastEvent in download() when next touching the store.
Parked (final): GPU Toggle in Settings > Models lost its accessible name after as="div" — Ruling: real a11y regression, small; surfaced to the user as the first follow-up (pass the row text via Toggle's label prop).
