# Babelay 2.5단계: UI 리디자인(daisyUI) + 백그라운드 온보딩 + 오버레이 수정 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 메인 창을 고정 사이드바 + 설정 탭 구조로 바꾸고 daisyUI로 스타일링하며, 온보딩 모델 다운로드를 백그라운드 토스트로 옮기고, 오버레이 표시 모드·조정 모드·리사이즈를 고친다.

**Architecture:** 스타일은 daisyUI 5 커스텀 테마 둘(`babelay`/`babelay-light`)이 `data-theme`으로 전환된다. 프론트는 `useModels`에 다운로드 큐를, `src/lib/overlay.ts`에 표시 모드 규칙을 순수 함수로 둔다. 백엔드 변경은 `models::delete`(선택 비우기)와 온보딩 창 옵션뿐이며 새 커맨드는 없다.

**Tech Stack:** Rust 1.98, Tauri 2.11, React 19, react-router 7, zustand 5, Tailwind 4, daisyUI 5.7, vitest 4, i18next. yarn 4.18.0 + Node 24는 `.mise.toml`이 고정.

**Spec:** `docs/superpowers/specs/2026-09-03-phase2.5-ui-onboarding-design.md` (상위: `docs/superpowers/specs/2026-09-02-babelay-design.md`)

## Global Constraints

- 대상 OS macOS 14.2+ / Windows 10+. 앱 식별자 `com.babelay.app`. 현재 브랜치(`main`)에서 작업.
- 셸에 mise가 활성화되어 있지 않으면 모든 yarn 명령은 `mise exec -- yarn …`. 빌드·검증은 로컬에서만(CI 없음).
- 검증 게이트(모든 태스크 공통, 해당하는 것만): `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --all -- --check`, `mise exec -- yarn tsc --noEmit`, `mise exec -- yarn test`, `mise exec -- yarn build`.
- UI 라이브러리는 daisyUI 5만. 아이콘 라이브러리 금지 — 아이콘은 `src/components/icons.tsx`의 인라인 SVG.
- 색: 초록 `#1ed760`은 "채우기 + 검정 글자"로만(`btn-primary`, `badge-primary`). 보조 텍스트 `#8a8a8a`(다크)/`#6a6a6a`(라이트) = `text-fg-muted`. 카드 반경 12px, 컨트롤은 pill. 안내 문장·힌트 문구 없음(제목과 컨트롤만; 툴팁은 허용).
- 세 로케일 파일(`src/locales/{ko,en,ja}.json`)의 키 집합은 항상 동일(테스트가 강제). "전사"라는 낱말 대신 세 로케일 모두 `Transcript`.
- `babelay-engine`은 Tauri에 의존하지 않는다(이번 계획에서 엔진 변경 없음).
- 동시 다운로드 1개(백엔드). 큐는 프론트가 든다.
- 오버레이 저장 값은 `x_ratio, y_ratio, w_ratio`뿐. 높이 비율 추가 금지.
- 설정 스키마 변경 없음. `asr.source_lang`, `overlay.subtitle_lang` 키 그대로 사용.
- 커밋 접두어 `feat:`/`fix:`/`refactor:`/`test:`/`docs:`. 메시지 끝에 빈 줄 후 두 줄:
  `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` 와
  `Claude-Session: https://claude.ai/code/session_012Aeo55pSd6RJzHDfMWPi5h`.

---

## 파일 구조

```
package.json                       # daisyui 추가
vite.config.ts                     # import.meta.env.PACKAGE_VERSION define
src/vite-env.d.ts                  # PACKAGE_VERSION 타입
src/index.css                      # daisyUI 플러그인 + 테마 2개 + fg-muted 토큰 + wide 브레이크포인트
src/lib/
├─ theme.ts                        # data-theme 전환
├─ overlay.ts                      # overlayLines() (신규)
├─ models.ts                       # queue/enqueue/dequeue, ERROR_KEYS 갱신
└─ i18n.ts                         # 변경 없음
src/components/
├─ PillButton.tsx, Toggle.tsx, Badge.tsx, ProgressBar.tsx   # 삭제
├─ SegmentedControl.tsx            # daisyUI join으로 재작성(얇은 래퍼는 유지)
├─ SettingGroup.tsx                # 토큰 갱신
├─ ErrorBar.tsx                    # alert alert-error
├─ ModelRow.tsx                    # btn/badge/progress, 사용 중 삭제 + ConfirmModal
├─ ConfirmModal.tsx                # 신규 (<dialog class="modal">)
├─ DownloadToast.tsx               # 신규 (toast toast-top toast-end)
├─ PermissionRow.tsx               # 신규
├─ Sidebar.tsx                     # 교체 (A 구조)
└─ icons.tsx                       # search/info/help/trash/check/x 추가
src/pages/
├─ MainApp.tsx                     # 라우트 갱신, DownloadToast
├─ Settings.tsx                    # 신규: 탭 셸
├─ main/Live.tsx                   # 헤더에서 시작/정지 제거, 오버레이 join
├─ main/History.tsx                # ?q= 초기 검색어, btn 클래스
├─ settings/{General,Models,Translation,Overlay}.tsx   # 탭 내용
├─ Onboarding.tsx                  # steps, 백그라운드 다운로드, 체크리스트
└─ OverlayWindow.tsx               # overlayLines, 예시 문구, 포인터 리사이즈
src/locales/{ko,en,ja}.json        # 키 정리
src/test/
├─ theme-dom.test.ts               # data-theme 검사
├─ models.test.ts                  # rowAction in_use→delete, ERROR_KEYS
├─ models-queue.test.ts            # 신규
└─ overlay.test.ts                 # 신규
src-tauri/src/
├─ models.rs                       # delete: 선택 비우기, 캡처 중 거부
└─ windows.rs                      # 온보딩 창 resizable + min size
```

---

### Task 1: daisyUI 도입과 테마 전환

**Files:**
- Modify: `package.json`, `src/index.css`, `src/lib/theme.ts`, `src/test/theme-dom.test.ts`

**Interfaces:**
- Produces: `applyTheme(pref)`가 `document.documentElement.dataset.theme`를 `"babelay" | "babelay-light"`로 설정. Tailwind 유틸리티 `text-fg-muted`, `bg-fg-muted`, 브레이크포인트 변형 `wide:`(≥800px).

- [ ] **Step 1: daisyUI 설치**

```bash
mise exec -- yarn add -D daisyui@^5.7
```

- [ ] **Step 2: 테마 DOM 테스트를 data-theme 검사로 바꾼다**

`src/test/theme-dom.test.ts`에서 `isDark` 정의와 단언을 아래로 교체:

```ts
    const theme = () => document.documentElement.dataset.theme;

    applyTheme("dark");
    expect(theme()).toBe("babelay");
    applyTheme("light");
    expect(theme()).toBe("babelay-light");
    applyTheme("system"); // matches: true
    expect(theme()).toBe("babelay");

    expect(matchMedia).toHaveBeenCalledTimes(1);
    expect(assigned).toBe(1);

    // 등록된 핸들러는 최신 pref를 읽는다.
    mql.matches = false;
    handler!();
    expect(theme()).toBe("babelay-light");
```

- [ ] **Step 3: 실패 확인**

Run: `mise exec -- yarn test src/test/theme-dom.test.ts`
Expected: FAIL — `expected undefined to be 'babelay'`

- [ ] **Step 4: theme.ts 수정**

`src/lib/theme.ts`의 `apply()`를 교체:

```ts
function apply() {
  const dark = resolveTheme(currentPref, mq?.matches ?? false) === "dark";
  document.documentElement.dataset.theme = dark ? "babelay" : "babelay-light";
}
```

- [ ] **Step 5: index.css 교체**

`src/index.css` 전체:

```css
@import "tailwindcss";
@source not "../docs";
@plugin "daisyui" { themes: false; exclude: rootcolor; }
@plugin "daisyui/theme" {
  name: "babelay"; default: true; color-scheme: dark;
  --color-base-100: #121212; --color-base-200: #181818; --color-base-300: #1f1f1f; --color-base-content: #ffffff;
  --color-neutral: #252525; --color-neutral-content: #ffffff;
  --color-primary: #1ed760; --color-primary-content: #000000;
  --color-secondary: #252525; --color-secondary-content: #ffffff;
  --color-accent: #1ed760; --color-accent-content: #000000;
  --color-info: #539df5; --color-info-content: #000000;
  --color-success: #1ed760; --color-success-content: #000000;
  --color-warning: #ffa42b; --color-warning-content: #000000;
  --color-error: #f3727f; --color-error-content: #000000;
  --radius-box: 12px; --radius-field: 9999px; --radius-selector: 9999px;
  --size-field: 0.25rem; --size-selector: 0.25rem; --border: 1px; --depth: 0; --noise: 0;
}
@plugin "daisyui/theme" {
  name: "babelay-light"; color-scheme: light;
  --color-base-100: #ffffff; --color-base-200: #f5f5f5; --color-base-300: #eeeeee; --color-base-content: #121212;
  --color-neutral: #e4e4e4; --color-neutral-content: #121212;
  --color-primary: #1ed760; --color-primary-content: #000000;
  --color-secondary: #e4e4e4; --color-secondary-content: #121212;
  --color-accent: #1ed760; --color-accent-content: #000000;
  --color-info: #539df5; --color-info-content: #000000;
  --color-success: #1ed760; --color-success-content: #000000;
  --color-warning: #ffa42b; --color-warning-content: #000000;
  --color-error: #d33a4a; --color-error-content: #ffffff;
  --radius-box: 12px; --radius-field: 9999px; --radius-selector: 9999px;
  --size-field: 0.25rem; --size-selector: 0.25rem; --border: 1px; --depth: 0; --noise: 0;
}

@theme {
  --color-fg-muted: #8a8a8a;
  --breakpoint-wide: 800px;
  --font-sans: -apple-system, "Helvetica Neue", Arial, "Hiragino Sans", "Apple SD Gothic Neo", "Meiryo", sans-serif;
}
[data-theme="babelay-light"] { --color-fg-muted: #6a6a6a; }

html, body, #root { height: 100%; margin: 0; }
body { color: var(--color-base-content); font-family: var(--font-sans); font-size: 14px; }
body:not(.overlay) { background: var(--color-base-100); }
```

`exclude: rootcolor`는 daisyUI가 `html`에 배경을 칠하지 않게 한다(오버레이 창 투명 유지).

- [ ] **Step 6: 테스트·타입·빌드 확인**

Run: `mise exec -- yarn test && mise exec -- yarn tsc --noEmit && mise exec -- yarn build`
Expected: 모두 PASS. 빌드 산출물 CSS에 `.btn`이 포함되는지 `grep -c "\.btn" dist/assets/*.css`로 확인(1 이상).

- [ ] **Step 7: 커밋**

```bash
git add package.json yarn.lock src/index.css src/lib/theme.ts src/test/theme-dom.test.ts
git commit -m "feat(ui): daisyUI themes from design tokens, data-theme switching"
```

---

### Task 2: 로케일 키 정리

**Files:**
- Modify: `src/locales/ko.json`, `src/locales/en.json`, `src/locales/ja.json`, `src/lib/models.ts:33-42`, `src/test/models.test.ts`

**Interfaces:**
- Produces: 아래 키 집합. 이후 태스크는 이 키만 쓴다.
  - 추가: `common.confirm, common.cancel, common.close, nav.search, nav.about, about.version, about.hardware, live.overlayOn, live.overlayOff, general.shortcuts, general.permission, permission.name, permission.path, permission.check, permission.openSettings, permission.granted, permission.denied, permission.unknown, models.confirmDelete, downloads.downloading, downloads.next, translation.sourceLang, translation.targetLang, translation.auto, overlay.preview, errors.capturing, onboarding.step.{language,permission,asr,llm,done}, onboarding.check.asr, onboarding.check.llm, onboarding.skipped`
  - 삭제: `models.continue, models.continueWith, overlay.subtitleLang, overlay.sourceLang, overlay.auto, errors.inUse, onboarding.permissionCheck, onboarding.permissionGranted, onboarding.permissionDenied, onboarding.permissionUnknown, onboarding.openSettings`
  - 변경: `models.asr` = "Transcript"(세 로케일), `onboarding.title.asr`, `overlay.adjustHint`, `errors.modelMissing`
- `ERROR_KEYS`: `in_use` 제거, `capturing: "errors.capturing"` 추가.

- [ ] **Step 1: ko.json 전체 교체**

```json
{
  "app": { "name": "Babelay" },
  "common": { "dismiss": "닫기", "confirm": "확인", "cancel": "취소", "close": "닫기" },
  "nav": { "live": "라이브", "history": "히스토리", "settings": "설정", "collapse": "사이드바 접기", "expand": "사이드바 펼치기", "search": "히스토리 검색", "about": "정보" },
  "about": { "version": "버전", "hardware": "하드웨어" },
  "live": { "start": "시작", "stop": "정지", "overlay": "오버레이", "overlayOn": "켬", "overlayOff": "끔", "stopping": "정지 중…", "cpuFallback": "CPU", "lagging": "지연" },
  "history": { "empty": "아직 세션이 없습니다.", "search": "검색", "exportTxt": "TXT", "exportSrt": "SRT", "delete": "삭제", "saved": "저장됨: {{path}}", "segments": "{{count}}개 조각" },
  "settings": { "general": "일반", "models": "모델", "translation": "번역", "overlay": "오버레이" },
  "general": {
    "theme": "테마", "themeSystem": "시스템", "themeDark": "다크", "themeLight": "라이트",
    "language": "언어", "langSystem": "시스템 기본", "langKo": "한국어", "langEn": "영어", "langJa": "일본어",
    "shortcuts": "단축키", "shortcutCapture": "캡처 시작 / 정지", "shortcutOverlay": "오버레이 표시 / 숨김",
    "permission": "권한"
  },
  "permission": {
    "name": "시스템 오디오 녹음", "path": "macOS · 개인정보 보호 및 보안 › 시스템 오디오 녹음",
    "check": "권한 확인", "openSettings": "시스템 설정 열기",
    "granted": "허용됨", "denied": "거부됨", "unknown": "확인 안 됨"
  },
  "models": {
    "desc": { "tiny": "가장 빠름 · 정확도 낮음", "base": "빠름 · 짧은 문장에 적합", "small": "속도·정확도 균형", "medium": "높은 정확도", "large_v3_turbo": "높은 정확도 · 다국어 강함", "large_v3": "최고 정확도", "gemma3_1b": "가장 빠름 · 단순 문장", "qwen3_5_2b": "속도·품질 균형", "gemma3_4b": "더 자연스러운 문장", "qwen3_5_4b": "최고 품질 · 한·일 강함" },
    "badgeInstalled": "설치됨", "badgeInUse": "사용 중", "badgeRecommended": "추천",
    "asr": "Transcript", "llm": "번역", "downloading": "다운로드 중", "cancel": "취소",
    "speed1": "매우 느림", "speed2": "느림", "speed3": "보통", "speed4": "빠름", "speed5": "매우 빠름",
    "gpuMac": "Apple Silicon 가속", "gpuWin": "NVIDIA GPU 가속",
    "download": "다운로드", "select": "선택", "delete": "삭제", "confirmDelete": "{{name}}을(를) 삭제할까요? ({{size}})"
  },
  "downloads": { "downloading": "{{name}} 다운로드 중", "next": "다음: {{name}}" },
  "translation": {
    "local": "로컬 모델", "cloud": "클라우드 API",
    "currentModel": "현재 모델", "changeInModels": "모델 탭에서 변경",
    "provider": "프로바이더", "model": "모델", "baseUrl": "Base URL",
    "providerOpenai": "OpenAI", "providerAnthropic": "Anthropic", "providerGemini": "Google Gemini", "providerDeepl": "DeepL", "providerCustom": "커스텀 (OpenAI 호환)",
    "sourceLang": "원어", "targetLang": "타겟 언어", "auto": "자동 감지"
  },
  "overlay": {
    "adjust": "위치 조정", "adjustDone": "조정 완료", "preview": "미리보기",
    "adjustHint": "드래그로 이동 · 모서리로 폭 조절",
    "displayMode": "표시 모드", "modeBoth": "원문 + 번역", "modeSource": "원문만", "modeTarget": "번역만",
    "fontSize": "글자 크기", "bgOpacity": "배경 투명도",
    "previewSource": "We buffer about two seconds of audio before running the model.",
    "previewTarget": "모델을 돌리기 전에 약 2초 분량의 오디오를 버퍼링합니다."
  },
  "errors": {
    "busy": "다른 모델을 내려받는 중입니다", "capturing": "캡처 중에는 모델을 삭제할 수 없습니다",
    "notDownloading": "진행 중인 다운로드가 없습니다", "unknownModel": "알 수 없는 모델입니다",
    "modelMissing": "선택한 Transcript 모델이 설치되어 있지 않습니다", "startFailed": "캡처를 시작할 수 없습니다",
    "busyStopping": "정지 중입니다. 잠시 후 다시 시도하세요", "historyUnavailable": "히스토리를 사용할 수 없습니다"
  },
  "onboarding": {
    "next": "다음", "back": "이전", "skip": "건너뛰기", "finish": "시작하기", "skipped": "건너뜀",
    "step": { "language": "언어", "permission": "권한", "asr": "Transcript", "llm": "번역", "done": "완료" },
    "title": { "language": "언어", "permission": "시스템 오디오 캡처 허용", "asr": "Transcript 모델", "llm": "번역 모델", "done": "준비 완료" },
    "check": { "asr": "Transcript 모델", "llm": "번역 모델" }
  }
}
```

- [ ] **Step 2: en.json 전체 교체**

```json
{
  "app": { "name": "Babelay" },
  "common": { "dismiss": "Dismiss", "confirm": "Confirm", "cancel": "Cancel", "close": "Close" },
  "nav": { "live": "Live", "history": "History", "settings": "Settings", "collapse": "Collapse sidebar", "expand": "Expand sidebar", "search": "Search history", "about": "About" },
  "about": { "version": "Version", "hardware": "Hardware" },
  "live": { "start": "Start", "stop": "Stop", "overlay": "Overlay", "overlayOn": "On", "overlayOff": "Off", "stopping": "Stopping…", "cpuFallback": "CPU", "lagging": "Lagging" },
  "history": { "empty": "No sessions yet.", "search": "Search", "exportTxt": "TXT", "exportSrt": "SRT", "delete": "Delete", "saved": "Saved: {{path}}", "segments": "{{count}} segments" },
  "settings": { "general": "General", "models": "Models", "translation": "Translation", "overlay": "Overlay" },
  "general": {
    "theme": "Theme", "themeSystem": "System", "themeDark": "Dark", "themeLight": "Light",
    "language": "Language", "langSystem": "System default", "langKo": "Korean", "langEn": "English", "langJa": "Japanese",
    "shortcuts": "Shortcuts", "shortcutCapture": "Start / stop capture", "shortcutOverlay": "Show / hide overlay",
    "permission": "Permission"
  },
  "permission": {
    "name": "System Audio Recording", "path": "macOS · Privacy & Security › System Audio Recording",
    "check": "Check permission", "openSettings": "Open System Settings",
    "granted": "Granted", "denied": "Denied", "unknown": "Not checked"
  },
  "models": {
    "desc": { "tiny": "fastest, low accuracy", "base": "fast, short sentences", "small": "balanced speed and accuracy", "medium": "high accuracy", "large_v3_turbo": "high accuracy, strong multilingual", "large_v3": "best accuracy", "gemma3_1b": "fastest, simple sentences", "qwen3_5_2b": "good balance", "gemma3_4b": "better fluency", "qwen3_5_4b": "best quality, strong CJK" },
    "badgeInstalled": "installed", "badgeInUse": "in use", "badgeRecommended": "recommended",
    "asr": "Transcript", "llm": "Translation", "downloading": "downloading", "cancel": "Cancel",
    "speed1": "Very slow", "speed2": "Slow", "speed3": "Medium", "speed4": "Fast", "speed5": "Very fast",
    "gpuMac": "Apple Silicon acceleration", "gpuWin": "NVIDIA GPU acceleration",
    "download": "Download", "select": "Select", "delete": "Delete", "confirmDelete": "Delete {{name}}? ({{size}})"
  },
  "downloads": { "downloading": "Downloading {{name}}", "next": "Next: {{name}}" },
  "translation": {
    "local": "Local model", "cloud": "Cloud API",
    "currentModel": "Current model", "changeInModels": "Change in Models",
    "provider": "Provider", "model": "Model", "baseUrl": "Base URL",
    "providerOpenai": "OpenAI", "providerAnthropic": "Anthropic", "providerGemini": "Google Gemini", "providerDeepl": "DeepL", "providerCustom": "Custom (OpenAI compatible)",
    "sourceLang": "Source language", "targetLang": "Target language", "auto": "Auto detect"
  },
  "overlay": {
    "adjust": "Adjust position", "adjustDone": "Done", "preview": "Preview",
    "adjustHint": "Drag to move · corner to resize width",
    "displayMode": "Display", "modeBoth": "Source + translation", "modeSource": "Source only", "modeTarget": "Translation only",
    "fontSize": "Font size", "bgOpacity": "Background opacity",
    "previewSource": "We buffer about two seconds of audio before running the model.",
    "previewTarget": "The model runs after buffering about two seconds of audio."
  },
  "errors": {
    "busy": "Another download is in progress", "capturing": "Models cannot be deleted while capturing",
    "notDownloading": "No download in progress", "unknownModel": "Unknown model",
    "modelMissing": "The selected Transcript model is not installed", "startFailed": "Could not start capture",
    "busyStopping": "Stopping — try again in a moment", "historyUnavailable": "History is unavailable"
  },
  "onboarding": {
    "next": "Next", "back": "Back", "skip": "Skip", "finish": "Get started", "skipped": "Skipped",
    "step": { "language": "Language", "permission": "Permission", "asr": "Transcript", "llm": "Translation", "done": "Done" },
    "title": { "language": "Language", "permission": "Allow system audio capture", "asr": "Transcript model", "llm": "Translation model", "done": "All set" },
    "check": { "asr": "Transcript model", "llm": "Translation model" }
  }
}
```

- [ ] **Step 3: ja.json 전체 교체**

```json
{
  "app": { "name": "Babelay" },
  "common": { "dismiss": "閉じる", "confirm": "確認", "cancel": "キャンセル", "close": "閉じる" },
  "nav": { "live": "ライブ", "history": "履歴", "settings": "設定", "collapse": "サイドバーを閉じる", "expand": "サイドバーを開く", "search": "履歴を検索", "about": "情報" },
  "about": { "version": "バージョン", "hardware": "ハードウェア" },
  "live": { "start": "開始", "stop": "停止", "overlay": "オーバーレイ", "overlayOn": "オン", "overlayOff": "オフ", "stopping": "停止中…", "cpuFallback": "CPU", "lagging": "遅延" },
  "history": { "empty": "まだセッションがありません。", "search": "検索", "exportTxt": "TXT", "exportSrt": "SRT", "delete": "削除", "saved": "保存済み: {{path}}", "segments": "{{count}} セグメント" },
  "settings": { "general": "一般", "models": "モデル", "translation": "翻訳", "overlay": "オーバーレイ" },
  "general": {
    "theme": "テーマ", "themeSystem": "システム", "themeDark": "ダーク", "themeLight": "ライト",
    "language": "言語", "langSystem": "システム既定", "langKo": "韓国語", "langEn": "英語", "langJa": "日本語",
    "shortcuts": "ショートカット", "shortcutCapture": "キャプチャ開始 / 停止", "shortcutOverlay": "オーバーレイ表示 / 非表示",
    "permission": "権限"
  },
  "permission": {
    "name": "システムオーディオ録音", "path": "macOS · プライバシーとセキュリティ › システムオーディオ録音",
    "check": "権限を確認", "openSettings": "システム設定を開く",
    "granted": "許可済み", "denied": "拒否", "unknown": "未確認"
  },
  "models": {
    "desc": { "tiny": "最速・精度は低い", "base": "速い・短い文向け", "small": "速度と精度のバランス", "medium": "高精度", "large_v3_turbo": "高精度・多言語に強い", "large_v3": "最高精度", "gemma3_1b": "最速・単純な文向け", "qwen3_5_2b": "バランス良好", "gemma3_4b": "より自然な文", "qwen3_5_4b": "最高品質・日中韓に強い" },
    "badgeInstalled": "インストール済み", "badgeInUse": "使用中", "badgeRecommended": "おすすめ",
    "asr": "Transcript", "llm": "翻訳", "downloading": "ダウンロード中", "cancel": "キャンセル",
    "speed1": "とても遅い", "speed2": "遅い", "speed3": "普通", "speed4": "速い", "speed5": "とても速い",
    "gpuMac": "Apple Silicon アクセラレーション", "gpuWin": "NVIDIA GPU アクセラレーション",
    "download": "ダウンロード", "select": "選択", "delete": "削除", "confirmDelete": "{{name}} を削除しますか？ ({{size}})"
  },
  "downloads": { "downloading": "{{name}} をダウンロード中", "next": "次: {{name}}" },
  "translation": {
    "local": "ローカルモデル", "cloud": "クラウド API",
    "currentModel": "現在のモデル", "changeInModels": "モデルタブで変更",
    "provider": "プロバイダー", "model": "モデル", "baseUrl": "Base URL",
    "providerOpenai": "OpenAI", "providerAnthropic": "Anthropic", "providerGemini": "Google Gemini", "providerDeepl": "DeepL", "providerCustom": "カスタム (OpenAI 互換)",
    "sourceLang": "音声の言語", "targetLang": "翻訳先の言語", "auto": "自動検出"
  },
  "overlay": {
    "adjust": "位置を調整", "adjustDone": "完了", "preview": "プレビュー",
    "adjustHint": "ドラッグで移動 · 角で幅を調整",
    "displayMode": "表示", "modeBoth": "原文 + 翻訳", "modeSource": "原文のみ", "modeTarget": "翻訳のみ",
    "fontSize": "文字サイズ", "bgOpacity": "背景の不透明度",
    "previewSource": "We buffer about two seconds of audio before running the model.",
    "previewTarget": "モデルを実行する前に約2秒分の音声をバッファします。"
  },
  "errors": {
    "busy": "別のモデルをダウンロード中です", "capturing": "キャプチャ中はモデルを削除できません",
    "notDownloading": "進行中のダウンロードはありません", "unknownModel": "不明なモデルです",
    "modelMissing": "選択した Transcript モデルがインストールされていません", "startFailed": "キャプチャを開始できません",
    "busyStopping": "停止中です。しばらくしてからもう一度お試しください", "historyUnavailable": "履歴を利用できません"
  },
  "onboarding": {
    "next": "次へ", "back": "戻る", "skip": "スキップ", "finish": "始める", "skipped": "スキップ済み",
    "step": { "language": "言語", "permission": "権限", "asr": "Transcript", "llm": "翻訳", "done": "完了" },
    "title": { "language": "言語", "permission": "システム音声のキャプチャを許可", "asr": "Transcript モデル", "llm": "翻訳モデル", "done": "準備完了" },
    "check": { "asr": "Transcript モデル", "llm": "翻訳モデル" }
  }
}
```

- [ ] **Step 4: ERROR_KEYS 갱신**

`src/lib/models.ts`의 `ERROR_KEYS`에서 `in_use: "errors.inUse",` 줄을 삭제하고 `capturing: "errors.capturing",`를 추가한다.

- [ ] **Step 5: 테스트 실행**

Run: `mise exec -- yarn test`
Expected: PASS (`locales.test.ts`가 키 집합 일치를, `models.test.ts`의 ERROR_KEYS 검사가 새 키를 확인). 이 시점에서 페이지들이 삭제된 키를 참조하지만 런타임에서 키 문자열이 그대로 보일 뿐 테스트·타입은 통과한다. 다음 태스크들이 참조를 지운다.

- [ ] **Step 6: 커밋**

```bash
git add src/locales src/lib/models.ts
git commit -m "refactor(i18n): phase 2.5 key set (Transcript, permission, downloads, translation langs)"
```

---

### Task 3: 기본 컴포넌트를 daisyUI 클래스로 교체

**Files:**
- Delete: `src/components/PillButton.tsx`, `src/components/Toggle.tsx`, `src/components/Badge.tsx`, `src/components/ProgressBar.tsx`
- Modify: `src/components/SegmentedControl.tsx`, `src/components/SettingGroup.tsx`, `src/components/ErrorBar.tsx`, `src/components/ModelRow.tsx`, `src/components/icons.tsx`, `src/pages/main/Live.tsx`, `src/pages/main/History.tsx`, `src/pages/settings/General.tsx`, `src/pages/settings/Models.tsx`, `src/pages/settings/Translation.tsx`, `src/pages/settings/Overlay.tsx`, `src/pages/Onboarding.tsx`, `src/pages/OverlayWindow.tsx`

**Interfaces:**
- Produces: `SegmentedControl<T>({ value, options, onChange })`(시그니처 동일, daisyUI join). `SettingGroup`, `SettingRow` 시그니처 동일. `Icon` 이름 추가: `search, info, help, trash, check, x`.
- 이 태스크는 시각적 교체만 한다. 동작 변경 없음.

- [ ] **Step 1: 아이콘 추가**

`src/components/icons.tsx`의 `paths`에 추가:

```ts
  search: "M11 4a7 7 0 1 0 0 14 7 7 0 0 0 0-14z M20 20l-4-4",
  info: "M12 3a9 9 0 1 0 0 18 9 9 0 0 0 0-18z M12 11v5 M12 8h.01",
  help: "M12 3a9 9 0 1 0 0 18 9 9 0 0 0 0-18z M9.5 9.5a2.5 2.5 0 1 1 3.5 2.3c-.7.4-1 1-1 1.7 M12 17h.01",
  trash: "M4 7h16 M10 11v6 M14 11v6 M6 7l1 13h10l1-13 M9 7V4h6v3",
  check: "M5 12l4 4L19 7",
  x: "M6 6l12 12 M18 6L6 18",
```

- [ ] **Step 2: SegmentedControl 재작성**

`src/components/SegmentedControl.tsx` 전체:

```tsx
interface Props<T extends string> {
  value: T;
  options: { value: T; label: string }[];
  onChange: (v: T) => void;
}

// daisyUI join. 얇은 래퍼를 남기는 이유는 네 곳에서 같은 마크업을 반복하지 않기 위해서다.
export function SegmentedControl<T extends string>({ value, options, onChange }: Props<T>) {
  return (
    <div role="tablist" className="join">
      {options.map((o) => (
        <button
          key={o.value}
          type="button"
          role="tab"
          aria-selected={o.value === value}
          onClick={() => onChange(o.value)}
          className={`btn btn-sm join-item ${o.value === value ? "btn-primary" : "btn-ghost"}`}
        >
          {o.label}
        </button>
      ))}
    </div>
  );
}
```

- [ ] **Step 3: SettingGroup·ErrorBar**

`src/components/SettingGroup.tsx` 전체:

```tsx
import type { ReactNode } from "react";

export function SettingGroup({ children }: { children: ReactNode }) {
  return <div className="divide-y divide-base-300 rounded-box bg-base-200">{children}</div>;
}

export function SettingRow({ label, as = "label", children }: { label: string; as?: "label" | "div"; children: ReactNode }) {
  const Tag = as;
  return (
    <Tag className="flex flex-wrap items-center justify-between gap-x-4 gap-y-2 px-4 py-3 text-sm">
      <span>{label}</span>
      <span className="flex items-center gap-2 text-fg-muted">{children}</span>
    </Tag>
  );
}
```

`src/components/ErrorBar.tsx`의 반환 JSX:

```tsx
    <div role="alert" className="alert alert-error mb-4 text-sm">
      <span className="min-w-0 break-words">{error}</span>
      <button type="button" onClick={clearError} aria-label={t("common.dismiss")} className="btn btn-ghost btn-xs">✕</button>
    </div>
```

- [ ] **Step 4: ModelRow 클래스 교체**

`src/components/ModelRow.tsx`에서 import 3줄(`Badge`, `PillButton`, `ProgressBar`)을 지우고 `button` 맵과 반환 JSX를 아래로 교체(동작은 Task 6에서 바꾼다):

```tsx
  const button = {
    download: <button type="button" className="btn btn-primary btn-sm" disabled={busy} onClick={() => download(info.id)}>{t("models.download")}</button>,
    cancel: <button type="button" className="btn btn-ghost btn-sm" onClick={() => cancel(info.id)}>{t("models.cancel")}</button>,
    select: (
      <div className="flex flex-wrap gap-1">
        <button type="button" className="btn btn-primary btn-sm" onClick={onSelect}>{t("models.select")}</button>
        <button type="button" className="btn btn-ghost btn-sm" onClick={() => remove(info.id)}>{t("models.delete")}</button>
      </div>
    ),
    delete: null,
  }[action];

  return (
    <div
      role="button"
      tabIndex={0}
      aria-pressed={selected}
      onClick={onSelect}
      onKeyDown={(e) => { if (e.target !== e.currentTarget) return; if (e.key === "Enter" || e.key === " ") { e.preventDefault(); onSelect(); } }}
      className={`grid grid-cols-[1fr_auto] items-center gap-3 rounded-box bg-base-200 px-4 py-3 text-left text-sm hover:bg-base-300 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary ${selected ? "ring-[1.5px] ring-inset ring-primary" : ""}`}
    >
      <div className="min-w-0">
        <div className="flex flex-wrap items-center gap-2 font-semibold">
          <span className="truncate">{info.name}</span>
          {status.in_use && status.installed && <span className="badge badge-primary badge-sm">{t("models.badgeInUse")}</span>}
          {status.installed && !status.in_use && <span className="badge badge-neutral badge-sm">{t("models.badgeInstalled")}</span>}
          {status.balanced && <span className="badge badge-neutral badge-sm">{t("models.badgeRecommended")}</span>}
        </div>
        <div className="mt-0.5 flex items-center gap-2 text-xs text-fg-muted">
          <span className="truncate">{meta}</span>
          {!status.download && (
            <span className="inline-flex gap-0.5" aria-label={t(`models.speed${info.speed}`)}>
              {[1, 2, 3, 4, 5].map((i) => <span key={i} className={`h-1.5 w-1.5 rounded-[2px] ${i <= info.speed ? "bg-fg-muted" : "bg-neutral"}`} />)}
            </span>
          )}
        </div>
        {status.download && <progress className="progress progress-primary mt-2 h-1 w-full" value={status.download.received} max={Math.max(1, status.download.total)} />}
      </div>
      <div onClick={(e) => e.stopPropagation()}>{button}</div>
    </div>
  );
```

- [ ] **Step 5: 페이지 호출부 교체**

각 파일에서 `PillButton`/`Toggle` import를 지우고:

`src/pages/main/Live.tsx` 헤더 버튼 두 개:
```tsx
          <button type="button" className="btn btn-primary btn-sm" disabled={view.stopping} onClick={() => (view.capturing ? stop() : start())}>
            {view.stopping ? t("live.stopping") : view.capturing ? `■ ${t("live.stop")}` : `● ${t("live.start")}`}
          </button>
          <button type="button" className={`btn btn-sm ${settings.overlay.enabled ? "btn-neutral" : "btn-ghost"}`} onClick={() => update({ overlay: { enabled: !settings.overlay.enabled } })}>{t("live.overlay")}</button>
```
타임라인 컨테이너의 `rounded-[var(--radius-card)] bg-base-2` → `rounded-box bg-base-200`.

`src/pages/main/History.tsx`: `PillButton variant="ghost"` → `<button type="button" className="btn btn-ghost btn-sm">`, 기본 `PillButton` → `btn btn-neutral btn-sm`, `variant="outline"` → `btn btn-outline btn-sm`. 검색 인풋 클래스 → `input input-sm w-56 rounded-full`. `rounded-[var(--radius-card)] bg-base-2` → `rounded-box bg-base-200`, `bg-surface`/`bg-base-2`/`bg-surface-2` → `bg-base-300`/`bg-base-200`/`bg-neutral`.

`src/pages/settings/General.tsx`: `const select = "select select-sm w-44";`. `<kbd>` → `<kbd className="kbd kbd-sm">`.

`src/pages/settings/Models.tsx`: `<Toggle checked=… onChange=…/>` → `<input type="checkbox" role="switch" className="toggle toggle-primary" checked={settings.asr.gpu} onChange={(e) => update({ asr: { gpu: e.target.checked } })} />`.

`src/pages/settings/Translation.tsx`: `const input = "input input-sm w-56";` 와 select에는 `select select-sm w-56`.

`src/pages/settings/Overlay.tsx`: `const select = "select select-sm w-44";`, 조정 버튼 → `<button type="button" className={`btn btn-sm ${adjust ? "btn-primary" : "btn-neutral"}`} …>`, range 두 개 → `className="range range-primary range-xs w-40"`, 미리보기 카드 `rounded-[var(--radius-card)]` → `rounded-box`.

`src/pages/Onboarding.tsx`: `PillButton variant="primary"` → `btn btn-primary btn-sm`, `variant="default"` → `btn btn-neutral btn-sm`, `variant="outline"` → `btn btn-outline btn-sm`, `variant="ghost"` → `btn btn-ghost btn-sm`. 진행 바 `bg-accent`/`bg-surface` → `bg-primary`/`bg-base-300`.

`src/pages/OverlayWindow.tsx`: `ring-accent` → `ring-primary`, `bg-accent … text-accent-fg` → `bg-primary … text-primary-content`(두 곳).

`src/components/Sidebar.tsx`(Task 9에서 교체하지만 지금 컴파일돼야 한다): `bg-base-2` → `bg-base-200`, `bg-surface-2` → `bg-neutral`, `bg-surface` → `bg-base-300`, `bg-accent` → `bg-primary`, `bg-base` → `bg-base-100`.

- [ ] **Step 6: 옛 컴포넌트 삭제와 잔여 참조 검사**

```bash
git rm src/components/PillButton.tsx src/components/Toggle.tsx src/components/Badge.tsx src/components/ProgressBar.tsx
grep -rn "PillButton\|<Toggle\|<Badge\|ProgressBar\|bg-surface\|bg-base-2\b\|bg-accent\|text-accent-fg\|radius-card\|radius-panel" src
```
Expected: grep 결과 없음.

- [ ] **Step 7: 검증**

Run: `mise exec -- yarn tsc --noEmit && mise exec -- yarn test && mise exec -- yarn build`
Expected: PASS.

- [ ] **Step 8: 커밋**

```bash
git add -A src
git commit -m "refactor(ui): replace hand-rolled controls with daisyUI classes"
```

---

### Task 4: 오버레이 표시 규칙, 조정 모드 예시 문구, 포인터 리사이즈

**Files:**
- Create: `src/lib/overlay.ts`, `src/test/overlay.test.ts`
- Modify: `src/pages/OverlayWindow.tsx`, `src/pages/settings/Overlay.tsx`

**Interfaces:**
- Produces:
  ```ts
  export interface OverlayLines { primary: string; secondary: string }
  export function overlayLines(mode: DisplayMode, source: string, partial: string, translated?: string): OverlayLines
  ```
  both/source → `{primary: source, secondary: partial}`, target → `{primary: translated ?? "", secondary: ""}`.

- [ ] **Step 1: 테스트 작성**

`src/test/overlay.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { overlayLines } from "../lib/overlay";

describe("overlayLines", () => {
  it("both shows source and partial", () => expect(overlayLines("both", "S", "P")).toEqual({ primary: "S", secondary: "P" }));
  it("source shows source and partial", () => expect(overlayLines("source", "S", "P")).toEqual({ primary: "S", secondary: "P" }));
  it("target is empty until a translation exists", () => expect(overlayLines("target", "S", "P")).toEqual({ primary: "", secondary: "" }));
  it("target shows the translation when given", () => expect(overlayLines("target", "S", "P", "T")).toEqual({ primary: "T", secondary: "" }));
});
```

- [ ] **Step 2: 실패 확인**

Run: `mise exec -- yarn test src/test/overlay.test.ts`
Expected: FAIL — 모듈 없음.

- [ ] **Step 3: 구현**

`src/lib/overlay.ts`:

```ts
import type { DisplayMode } from "./types";

export interface OverlayLines { primary: string; secondary: string }

/** 표시 모드 → 오버레이 두 줄. 3단계에서 translated 를 채우면 both 도 번역 줄을 얻는다. */
export function overlayLines(mode: DisplayMode, source: string, partial: string, translated = ""): OverlayLines {
  if (mode === "target") return { primary: translated, secondary: "" };
  return { primary: source, secondary: partial };
}
```

- [ ] **Step 4: 통과 확인**

Run: `mise exec -- yarn test src/test/overlay.test.ts`
Expected: PASS.

- [ ] **Step 5: OverlayWindow 재작성**

`src/pages/OverlayWindow.tsx` 전체:

```tsx
import { useEffect, useRef, useState, type PointerEvent as ReactPointerEvent } from "react";
import { getCurrentWindow, PhysicalSize } from "@tauri-apps/api/window";
import { listen } from "@tauri-apps/api/event";
import { useTranslation } from "react-i18next";
import { api } from "../lib/tauri";
import { overlayLines } from "../lib/overlay";
import { useSession } from "../lib/session";
import { useSettings } from "../lib/settings";

// 마지막 이벤트 후 이만큼 지나면 자막을 지운다.
const IDLE_MS = 6000;
// 물리 픽셀. 이보다 좁으면 글자 한 줄도 안 들어간다.
const MIN_WIDTH = 240;

/**
 * macOS 의 tao 는 startResizeDragging 을 지원하지 않는다(NotSupported).
 * 핸들에서 포인터를 잡고 이동량만큼 창 폭을 직접 바꾼다. 높이는 그대로.
 */
function startWidthResize(e: ReactPointerEvent<HTMLDivElement>) {
  if (e.button !== 0) return;
  e.stopPropagation();
  const handle = e.currentTarget;
  handle.setPointerCapture(e.pointerId);
  const win = getCurrentWindow();
  const startX = e.screenX;
  const scale = window.devicePixelRatio || 1;
  let raf = 0;
  let pending: number | null = null;
  win.innerSize().then(({ width: w0, height: h0 }) => {
    const move = (ev: PointerEvent) => {
      pending = Math.max(MIN_WIDTH, Math.round(w0 + (ev.screenX - startX) * scale));
      if (raf) return;
      raf = requestAnimationFrame(() => {
        raf = 0;
        if (pending !== null) win.setSize(new PhysicalSize(pending, h0)).catch(() => {});
      });
    };
    const up = () => {
      handle.removeEventListener("pointermove", move);
      handle.removeEventListener("pointerup", up);
      handle.removeEventListener("pointercancel", up);
      api.overlayCommitPosition().catch(useSettings.getState().setError);
    };
    handle.addEventListener("pointermove", move);
    handle.addEventListener("pointerup", up);
    handle.addEventListener("pointercancel", up);
  });
}

export default function OverlayWindow() {
  const { t } = useTranslation();
  const settings = useSettings((s) => s.settings);
  const view = useSession((s) => s.view);
  const [adjust, setAdjust] = useState(false);
  const [fresh, setFresh] = useState(false);
  const timer = useRef<number | undefined>(undefined);

  useEffect(() => {
    const un = listen<boolean>("overlay-adjust-mode", (e) => setAdjust(e.payload));
    return () => { un.then((f) => f()); };
  }, []);

  useEffect(() => {
    if (!view.lastEventAt) return;
    setFresh(true);
    const id = window.setTimeout(() => setFresh(false), IDLE_MS);
    return () => window.clearTimeout(id);
  }, [view.lastEventAt]);

  useEffect(() => {
    if (!adjust) return;
    const win = getCurrentWindow();
    const commit = () => {
      window.clearTimeout(timer.current);
      timer.current = window.setTimeout(
        () => api.overlayCommitPosition().catch(useSettings.getState().setError),
        300,
      );
    };
    const subs = [win.onMoved(commit), win.onResized(commit)];
    return () => {
      window.clearTimeout(timer.current);
      subs.forEach((p) => p.then((f) => f()));
    };
  }, [adjust]);

  if (!settings) return null;
  const { font_size, bg_opacity, display_mode } = settings.overlay;
  const last = view.finals[view.finals.length - 1]?.text ?? "";
  const partial = view.partial?.text ?? "";
  // 조정 모드에서 자막이 없으면 예시 문구로 상자를 채운다. 캡처 중이면 실제 자막이 우선.
  const sample = adjust && !view.capturing;
  const lines = sample
    ? overlayLines(display_mode, t("overlay.previewSource"), "", t("overlay.previewTarget"))
    : overlayLines(display_mode, last, partial);
  // 정지 뒤에는 마지막 자막이 다시 뜨면 안 된다(캡처 중일 때만 보인다).
  const visible = adjust || (view.capturing && fresh && Boolean(lines.primary || lines.secondary));

  return (
    <div className="flex h-full w-full items-end justify-center bg-transparent p-2">
      <div
        onMouseDown={(e) => { if (adjust && e.button === 0) getCurrentWindow().startDragging(); }}
        className={`relative rounded-[10px] px-4 py-2 text-center text-white transition-opacity duration-500 ${adjust ? "w-full min-h-12 cursor-move ring-2 ring-primary" : "max-w-full"}`}
        style={{ background: `rgba(18,18,18,${bg_opacity})`, backdropFilter: "blur(6px)", opacity: visible ? 1 : 0 }}
      >
        {lines.primary && <div style={{ fontSize: font_size, lineHeight: 1.3 }} className="font-bold">{lines.primary}</div>}
        {lines.secondary && <div style={{ fontSize: font_size * 0.6 }} className="text-white/70">{lines.secondary}</div>}
        {adjust && (
          <>
            <div className="absolute -top-6 left-0 rounded bg-primary px-2 py-0.5 text-xs font-bold text-primary-content">{t("overlay.adjustHint")}</div>
            <div
              onPointerDown={startWidthResize}
              onMouseDown={(e) => e.stopPropagation()}
              className="absolute -right-1.5 -bottom-1.5 h-3 w-3 cursor-ew-resize rounded-[2px] bg-primary"
            />
          </>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 6: 설정 › 오버레이 미리보기를 컨테이너 단위로**

`src/pages/settings/Overlay.tsx`에서 미리보기 카드 블록을 교체하고 `overlayLines`를 import:

```tsx
import { overlayLines } from "../../lib/overlay";
```

```tsx
      <div className="flex items-center justify-between">
        <span className="text-sm text-fg-muted">{t("overlay.preview")}</span>
        <button type="button" className={`btn btn-sm ${adjust ? "btn-primary" : "btn-neutral"}`} onClick={toggleAdjust}>{adjust ? t("overlay.adjustDone") : t("overlay.adjust")}</button>
      </div>

      {(() => {
        const lines = overlayLines(o.display_mode, t("overlay.previewSource"), "", t("overlay.previewTarget"));
        const scale = o.font_size / 28;
        return (
          <div className="@container relative aspect-[16/7] w-full rounded-box bg-[linear-gradient(#1b2230,#0e1218)]">
            <div className="absolute bottom-[6%] left-1/2 w-[60%] -translate-x-1/2 rounded-[1cqw] px-[3cqw] py-[2.5cqw] text-center text-white" style={{ background: `rgba(18,18,18,${o.bg_opacity})` }}>
              {lines.primary && <div className="font-bold leading-tight" style={{ fontSize: `${4.2 * scale}cqw` }}>{lines.primary}</div>}
              {lines.secondary && <div className="text-white/70" style={{ fontSize: `${2.6 * scale}cqw` }}>{lines.secondary}</div>}
            </div>
          </div>
        );
      })()}
```

제목 줄(`<h2>` + 조정 버튼)은 지운다(탭 셸이 제목을 가진다 — Task 8). 자막 언어·원어 두 `SettingRow`도 지운다(Task 7의 번역 탭으로 이동). `UiLang`, `SourceLang` import와 `select` 상수도 지운다. 그룹 1에는 표시 모드만 남는다.

- [ ] **Step 7: 검증**

Run: `mise exec -- yarn tsc --noEmit && mise exec -- yarn test && mise exec -- yarn build`
Expected: PASS.

- [ ] **Step 8: 커밋**

```bash
git add src/lib/overlay.ts src/test/overlay.test.ts src/pages/OverlayWindow.tsx src/pages/settings/Overlay.tsx
git commit -m "fix(overlay): honor display mode, sample text in adjust mode, pointer-driven width resize"
```

---

### Task 5: 백엔드 — 사용 중 모델 삭제, 온보딩 창 리사이즈

**Files:**
- Modify: `src-tauri/src/models.rs:213-232` (delete), 테스트 모듈, `src-tauri/src/windows.rs:35-46`

**Interfaces:**
- Produces: `pub fn clear_selection(settings: &mut Settings, id: &str) -> bool`. `delete`는 `in_use` 오류를 내지 않고, 캡처 중이면 `Err("capturing")`.

- [ ] **Step 1: 실패하는 테스트**

`src-tauri/src/models.rs` 테스트 모듈에 추가:

```rust
    #[test]
    fn clear_selection_empties_only_the_deleted_model() {
        let mut s = Settings::default();
        s.asr.model_id = "small".into();
        s.translation.local_model = "qwen3.5-2b".into();
        assert!(clear_selection(&mut s, "small"));
        assert_eq!(s.asr.model_id, "");
        assert_eq!(s.translation.local_model, "qwen3.5-2b");
        assert!(!clear_selection(&mut s, "large-v3"));
    }
```

- [ ] **Step 2: 실패 확인**

Run: `cargo test -p babelay clear_selection`
Expected: 컴파일 오류 — `clear_selection` 없음.

- [ ] **Step 3: 구현**

`src-tauri/src/models.rs`의 `delete`를 교체하고 그 위에 `clear_selection`을 둔다:

```rust
/// 삭제한 모델이 선택돼 있으면 선택을 비운다. 바뀌었으면 true.
pub fn clear_selection(settings: &mut Settings, id: &str) -> bool {
    let mut changed = false;
    if settings.asr.model_id == id {
        settings.asr.model_id.clear();
        changed = true;
    }
    if settings.translation.local_model == id {
        settings.translation.local_model.clear();
        changed = true;
    }
    changed
}

pub fn delete(app: &AppHandle, id: &str) -> Result<(), String> {
    let m = find(id).ok_or("unknown model")?;
    // 캡처 중에는 파일을 읽고 있다. 어느 모델이든 거부한다.
    if crate::session::is_capturing(app) {
        return Err("capturing".into());
    }
    if lock(&app.state::<Downloads>())
        .as_ref()
        .is_some_and(|a| a.id == id)
    {
        return Err("busy".into());
    }
    let path = model_path(&models_dir(app)?, m);
    let mut part = path.as_os_str().to_owned();
    part.push(".part");
    for p in [path, PathBuf::from(part)] {
        if p.exists() {
            std::fs::remove_file(&p).map_err(|e| e.to_string())?;
        }
    }
    let state = app.state::<SettingsState>();
    let mut s = state.get();
    if clear_selection(&mut s, id) {
        state.set(app, s)?;
    }
    Ok(())
}
```

- [ ] **Step 4: 온보딩 창**

`src-tauri/src/windows.rs`의 `show_onboarding` 빌더에서 `.resizable(false)`를 다음으로 교체:

```rust
        .inner_size(720.0, 560.0)
        .min_inner_size(640.0, 480.0)
```

- [ ] **Step 5: 검증**

Run: `cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --all -- --check`
Expected: PASS.

- [ ] **Step 6: 커밋**

```bash
git add src-tauri/src/models.rs src-tauri/src/windows.rs
git commit -m "feat(models): allow deleting the model in use (clears selection); resizable onboarding window"
```

---

### Task 6: ModelRow 삭제 확인 모달

**Files:**
- Create: `src/components/ConfirmModal.tsx`
- Modify: `src/components/ModelRow.tsx`, `src/test/models.test.ts`

**Interfaces:**
- Produces: `ConfirmModal({ open, message, onConfirm, onCancel })`. `rowAction`은 변경 없음(in_use → `"delete"`가 이미 그렇다); `ModelRow`가 `delete` 액션에 삭제 버튼을 그린다.

- [ ] **Step 1: 테스트 이름 갱신**

`src/test/models.test.ts`의 `"in use → delete (disabled by UI)"`를 `"in use → delete"`로 바꾼다(단언 동일). Run: `mise exec -- yarn test src/test/models.test.ts` → PASS.

- [ ] **Step 2: ConfirmModal**

`src/components/ConfirmModal.tsx`:

```tsx
import { useEffect, useRef } from "react";
import { useTranslation } from "react-i18next";

interface Props { open: boolean; message: string; onConfirm: () => void; onCancel: () => void }

export function ConfirmModal({ open, message, onConfirm, onCancel }: Props) {
  const { t } = useTranslation();
  const ref = useRef<HTMLDialogElement>(null);
  useEffect(() => {
    const d = ref.current;
    if (!d) return;
    if (open && !d.open) d.showModal();
    if (!open && d.open) d.close();
  }, [open]);
  return (
    <dialog ref={ref} className="modal" onClose={onCancel} onClick={(e) => e.stopPropagation()}>
      <div className="modal-box max-w-sm">
        <p className="text-sm">{message}</p>
        <div className="modal-action">
          <button type="button" className="btn btn-ghost btn-sm" onClick={onCancel}>{t("common.cancel")}</button>
          <button type="button" className="btn btn-error btn-sm" onClick={onConfirm}>{t("common.confirm")}</button>
        </div>
      </div>
    </dialog>
  );
}
```

- [ ] **Step 3: ModelRow에 삭제 흐름**

`src/components/ModelRow.tsx`:

```tsx
import { useState } from "react";
import { ConfirmModal } from "./ConfirmModal";
import { Icon } from "./icons";
```

컴포넌트 안에 `const [confirm, setConfirm] = useState(false);`를 두고 `button` 맵을 교체:

```tsx
  const deleteBtn = (
    <button type="button" className="btn btn-ghost btn-sm gap-1" aria-label={t("models.delete")} onClick={() => setConfirm(true)}>
      <Icon name="trash" />{t("models.delete")}
    </button>
  );
  const button = {
    download: <button type="button" className="btn btn-primary btn-sm" disabled={busy} onClick={() => download(info.id)}>{t("models.download")}</button>,
    cancel: <button type="button" className="btn btn-ghost btn-sm" onClick={() => cancel(info.id)}>{t("models.cancel")}</button>,
    select: (
      <div className="flex flex-wrap gap-1">
        <button type="button" className="btn btn-primary btn-sm" onClick={onSelect}>{t("models.select")}</button>
        {deleteBtn}
      </div>
    ),
    delete: deleteBtn,
  }[action];
```

버튼 셀을 교체:

```tsx
      <div onClick={(e) => e.stopPropagation()}>
        {button}
        <ConfirmModal
          open={confirm}
          message={t("models.confirmDelete", { name: info.name, size: formatSize(info.size_bytes) })}
          onCancel={() => setConfirm(false)}
          onConfirm={() => { setConfirm(false); remove(info.id); }}
        />
      </div>
```

- [ ] **Step 4: 검증**

Run: `mise exec -- yarn tsc --noEmit && mise exec -- yarn test && mise exec -- yarn build`
Expected: PASS.

- [ ] **Step 5: 커밋**

```bash
git add src/components/ConfirmModal.tsx src/components/ModelRow.tsx src/test/models.test.ts
git commit -m "feat(models): delete button on every installed model with confirm modal"
```

---

### Task 7: 다운로드 큐와 토스트

**Files:**
- Create: `src/components/DownloadToast.tsx`, `src/test/models-queue.test.ts`
- Modify: `src/lib/models.ts`, `src/pages/MainApp.tsx`

**Interfaces:**
- Produces (`useModels`):
  ```ts
  queue: string[];
  enqueue: (id: string) => Promise<void>;   // 받는 중 없음 → download(id); 있음 → 큐(같은 kind 교체)
  dequeue: (id: string) => void;
  ```
  `bind()`는 `done|error|cancelled` 후 `refresh()` 다음 큐 앞을 `download()`한다.
- `DownloadToast()`: props 없음. 받는 중·대기 없으면 null.

- [ ] **Step 1: 큐 테스트**

`src/test/models-queue.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from "vitest";
import type { DownloadEvent, ModelStatus } from "../lib/types";

const h = vi.hoisted(() => ({
  handler: null as ((e: { payload: DownloadEvent }) => void) | null,
  api: { getModels: vi.fn(), downloadModel: vi.fn(), cancelDownload: vi.fn(), deleteModel: vi.fn() },
}));
vi.mock("@tauri-apps/api/event", () => ({
  listen: vi.fn((_: string, cb: (e: { payload: DownloadEvent }) => void) => { h.handler = cb; return Promise.resolve(() => {}); }),
}));
vi.mock("../lib/tauri", () => ({ api: h.api }));

import { useModels } from "../lib/models";

const model = (id: string, kind: "asr" | "llm", download: ModelStatus["download"] = null): ModelStatus => ({
  info: { id, kind, name: id, desc_key: "models.desc.small", size_bytes: 10, speed: 3, url: "https://x", filename: id, sha256: null },
  installed: false, in_use: false, balanced: false, download,
});
const flush = () => new Promise((r) => setTimeout(r, 0));

beforeEach(() => {
  vi.clearAllMocks();
  h.api.getModels.mockResolvedValue([model("small", "asr"), model("qwen", "llm"), model("gemma", "llm")]);
  h.api.downloadModel.mockResolvedValue(undefined);
  useModels.setState({ models: [model("small", "asr"), model("qwen", "llm"), model("gemma", "llm")], queue: [], lastEvent: null });
});

describe("download queue", () => {
  it("starts immediately when nothing is downloading", async () => {
    await useModels.getState().enqueue("small");
    expect(h.api.downloadModel).toHaveBeenCalledWith("small");
    expect(useModels.getState().queue).toEqual([]);
  });

  it("queues while another download is active, replacing the same kind", async () => {
    useModels.setState({ models: [model("small", "asr", { received: 1, total: 10 }), model("qwen", "llm"), model("gemma", "llm")] });
    await useModels.getState().enqueue("qwen");
    await useModels.getState().enqueue("gemma");
    expect(h.api.downloadModel).not.toHaveBeenCalled();
    expect(useModels.getState().queue).toEqual(["gemma"]);
  });

  it("starts the next queued model after the active one finishes", async () => {
    useModels.setState({ models: [model("small", "asr", { received: 1, total: 10 }), model("qwen", "llm"), model("gemma", "llm")], queue: ["qwen"] });
    const unbind = useModels.getState().bind();
    await flush();
    h.handler!({ payload: { id: "small", received: 10, total: 10, state: "done", message: null } });
    await flush();
    expect(h.api.downloadModel).toHaveBeenCalledWith("qwen");
    expect(useModels.getState().queue).toEqual([]);
    unbind();
  });

  it("dequeue removes a waiting model", () => {
    useModels.setState({ queue: ["qwen", "gemma"] });
    useModels.getState().dequeue("qwen");
    expect(useModels.getState().queue).toEqual(["gemma"]);
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `mise exec -- yarn test src/test/models-queue.test.ts`
Expected: FAIL — `enqueue is not a function`.

- [ ] **Step 3: 스토어 구현**

`src/lib/models.ts`의 `ModelsStore` 인터페이스에 추가:

```ts
  queue: string[];
  enqueue: (id: string) => Promise<void>;
  dequeue: (id: string) => void;
```

`create` 본문에 추가/교체:

```ts
  queue: [],
  enqueue: async (id) => {
    const s = get();
    if (!s.models.some((m) => m.download)) { await s.download(id); return; }
    const kindOf = (x: string) => s.models.find((m) => m.info.id === x)?.info.kind;
    const kind = kindOf(id);
    // 온보딩에서 마음을 바꾸면 같은 종류의 대기 항목은 새 선택으로 갈아끼운다.
    set({ queue: [...s.queue.filter((q) => q !== id && kindOf(q) !== kind), id] });
  },
  dequeue: (id) => set({ queue: get().queue.filter((q) => q !== id) }),
```

`bind()`의 리스너에서 `if (state === "error" && message) report(message); get().refresh();`를 다음으로 교체:

```ts
      if (state === "error" && message) report(message);
      get().refresh().then(() => {
        const [next, ...rest] = get().queue;
        if (!next) return;
        set({ queue: rest });
        get().download(next);
      });
```

- [ ] **Step 4: 통과 확인**

Run: `mise exec -- yarn test src/test/models-queue.test.ts`
Expected: PASS.

- [ ] **Step 5: DownloadToast**

`src/components/DownloadToast.tsx`:

```tsx
import { useTranslation } from "react-i18next";
import { formatSize, useModels } from "../lib/models";

// 오른쪽 상단. 받는 중인 모델 카드 하나 + 대기 목록. 아무것도 없으면 그리지 않는다.
export function DownloadToast() {
  const { t } = useTranslation();
  const models = useModels((s) => s.models);
  const queue = useModels((s) => s.queue);
  const cancel = useModels((s) => s.cancel);
  const dequeue = useModels((s) => s.dequeue);
  const active = models.find((m) => m.download);
  if (!active?.download && queue.length === 0) return null;
  const name = (id: string) => models.find((m) => m.info.id === id)?.info.name ?? id;

  return (
    <div className="toast toast-top toast-end z-50">
      <div className="w-64 rounded-box bg-neutral p-3 text-sm text-neutral-content shadow-lg">
        {active?.download && (
          <>
            <div className="flex items-center justify-between gap-2 font-semibold">
              <span className="truncate">{t("downloads.downloading", { name: active.info.name })}</span>
              <button type="button" className="btn btn-ghost btn-xs" aria-label={t("models.cancel")} onClick={() => cancel(active.info.id)}>✕</button>
            </div>
            <div className="text-xs text-fg-muted">
              {Math.round((active.download.received / Math.max(1, active.download.total)) * 100)}% · {formatSize(active.download.received)} / {formatSize(active.download.total)}
            </div>
            <progress className="progress progress-primary mt-1 h-1 w-full" value={active.download.received} max={Math.max(1, active.download.total)} />
          </>
        )}
        {queue.map((id) => (
          <div key={id} className="mt-1 flex items-center justify-between gap-2 text-xs text-fg-muted">
            <span className="truncate">{t("downloads.next", { name: name(id) })}</span>
            <button type="button" className="btn btn-ghost btn-xs" aria-label={t("models.cancel")} onClick={() => dequeue(id)}>✕</button>
          </div>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 6: MainApp에 배치**

`src/pages/MainApp.tsx`에 `import { DownloadToast } from "../components/DownloadToast";`를 추가하고 `<Sidebar …/>` 앞에 `<DownloadToast />`를 넣는다(온보딩 창은 Task 10).

- [ ] **Step 7: 검증**

Run: `mise exec -- yarn tsc --noEmit && mise exec -- yarn test && mise exec -- yarn build`
Expected: PASS.

- [ ] **Step 8: 커밋**

```bash
git add src/lib/models.ts src/components/DownloadToast.tsx src/test/models-queue.test.ts src/pages/MainApp.tsx
git commit -m "feat(models): frontend download queue and top-right progress toast"
```

---

### Task 8: 권한 행, 일반 탭, 번역 탭

**Files:**
- Create: `src/components/PermissionRow.tsx`
- Modify: `src/pages/settings/General.tsx`, `src/pages/settings/Translation.tsx`

**Interfaces:**
- Produces: `PermissionRow({ onStatus? })` — 마운트 시 `api.checkAudioPermission()`을 한 번 호출하고 상태를 스스로 들며 `onStatus`로 알린다(온보딩과 설정 › 일반 공용). `PermissionIcon({ perm })`, `type Perm`.

- [ ] **Step 1: PermissionRow**

`src/components/PermissionRow.tsx`:

```tsx
import { useEffect, useState } from "react";
import { useTranslation } from "react-i18next";
import { Icon } from "./icons";
import { SettingGroup } from "./SettingGroup";
import { api } from "../lib/tauri";
import { useSettings } from "../lib/settings";

export type Perm = "granted" | "denied" | "unknown";

/** 상태 아이콘. 온보딩 완료 단계도 같은 표시를 쓴다. */
export function PermissionIcon({ perm }: { perm: Perm | null }) {
  const { t } = useTranslation();
  if (perm === "granted") return <span className="flex h-6 w-6 items-center justify-center rounded-full bg-primary text-primary-content" aria-label={t("permission.granted")}><Icon name="check" /></span>;
  if (perm === "denied") return <span className="flex h-6 w-6 items-center justify-center rounded-full bg-error text-error-content" aria-label={t("permission.denied")}><Icon name="x" /></span>;
  return <span className="flex h-6 w-6 items-center justify-center rounded-full bg-neutral text-neutral-content" aria-label={t("permission.unknown")}><Icon name="help" /></span>;
}

// 권한 조회는 실제 탭을 만들어 TCC 프롬프트를 띄운다. 마운트당 한 번만.
export function PermissionRow({ onStatus }: { onStatus?: (p: Perm) => void }) {
  const { t } = useTranslation();
  const setError = useSettings((s) => s.setError);
  const [perm, setPerm] = useState<Perm | null>(null);
  const check = () => api.checkAudioPermission().then((p) => { setPerm(p); onStatus?.(p); }).catch(setError);
  useEffect(() => { check(); }, []);

  return (
    <div className="flex flex-col gap-3">
      <SettingGroup>
        <div className="flex items-center justify-between gap-4 px-4 py-3 text-sm">
          <div className="min-w-0">
            <div className="font-semibold">{t("permission.name")}</div>
            <div className="truncate text-xs text-fg-muted">{t("permission.path")}</div>
          </div>
          <PermissionIcon perm={perm} />
        </div>
      </SettingGroup>
      <div className="flex flex-wrap gap-2">
        <button type="button" className={`btn btn-sm ${perm === "denied" ? "btn-neutral" : "btn-primary"}`} onClick={check}>{t("permission.check")}</button>
        <button type="button" className={`btn btn-sm ${perm === "denied" ? "btn-primary" : "btn-outline"}`} onClick={() => api.openPrivacySettings().catch(setError)}>{t("permission.openSettings")}</button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 일반 탭**

`src/pages/settings/General.tsx` 전체:

```tsx
import { useEffect, useState } from "react";
import { useTranslation } from "react-i18next";
import { Icon } from "../../components/icons";
import { PermissionRow } from "../../components/PermissionRow";
import { SettingGroup, SettingRow } from "../../components/SettingGroup";
import { resolveLang } from "../../lib/i18n";
import { useSettings } from "../../lib/settings";
import { api } from "../../lib/tauri";
import type { Theme, UiLang } from "../../lib/types";

const select = "select select-sm w-44";
const LANG_KEY = { ko: "general.langKo", en: "general.langEn", ja: "general.langJa" } as const;

export default function General() {
  const { t } = useTranslation();
  const { settings, update } = useSettings();
  const [platform, setPlatform] = useState("macos");
  useEffect(() => { api.getPlatform().then(setPlatform).catch(() => {}); }, []);
  if (!settings) return null;
  const systemLang = t(LANG_KEY[resolveLang("system", navigator.language)]);

  return (
    <div className="flex flex-col gap-4">
      <div className="flex justify-end">
        <div className="tooltip tooltip-left" data-tip={`${t("general.shortcutCapture")}: ⌘/Ctrl+Shift+S · ${t("general.shortcutOverlay")}: ⌘/Ctrl+Shift+O`}>
          <button type="button" className="btn btn-circle btn-ghost btn-sm" aria-label={t("general.shortcuts")}><Icon name="help" /></button>
        </div>
      </div>

      <SettingGroup>
        <SettingRow label={t("general.theme")}>
          <select className={select} value={settings.general.theme} onChange={(e) => update({ general: { theme: e.target.value as Theme } })}>
            <option value="system">{t("general.themeSystem")}</option>
            <option value="dark">{t("general.themeDark")}</option>
            <option value="light">{t("general.themeLight")}</option>
          </select>
        </SettingRow>
        <SettingRow label={t("general.language")}>
          <select className={select} value={settings.general.ui_language} onChange={(e) => update({ general: { ui_language: e.target.value as UiLang } })}>
            <option value="system">{`${t("general.langSystem")} (${systemLang})`}</option>
            <option value="ko">{t("general.langKo")}</option>
            <option value="en">{t("general.langEn")}</option>
            <option value="ja">{t("general.langJa")}</option>
          </select>
        </SettingRow>
      </SettingGroup>

      {platform === "macos" && (
        <>
          <div className="text-xs font-semibold uppercase tracking-wider text-fg-muted">{t("general.permission")}</div>
          <PermissionRow />
        </>
      )}
    </div>
  );
}
```

- [ ] **Step 3: 번역 탭에 원어·타겟 언어**

`src/pages/settings/Translation.tsx`: `<h2>` 줄을 지우고 최상위 `div`를 `flex flex-col gap-4`로. import에 `SourceLang, UiLang` 타입을 추가하고, 기존 조건부 `SettingGroup` 아래에 그룹을 추가:

```tsx
      <SettingGroup>
        <SettingRow label={t("translation.sourceLang")}>
          <select className="select select-sm w-44" value={settings.asr.source_lang} onChange={(e) => update({ asr: { source_lang: e.target.value as SourceLang } })}>
            <option value="auto">{t("translation.auto")}</option><option value="ko">{t("general.langKo")}</option><option value="en">{t("general.langEn")}</option><option value="ja">{t("general.langJa")}</option>
          </select>
        </SettingRow>
        <SettingRow label={t("translation.targetLang")}>
          <select className="select select-sm w-44" value={settings.overlay.subtitle_lang} onChange={(e) => update({ overlay: { subtitle_lang: e.target.value as UiLang } })}>
            <option value="system">{t("general.langSystem")}</option><option value="ko">{t("general.langKo")}</option><option value="en">{t("general.langEn")}</option><option value="ja">{t("general.langJa")}</option>
          </select>
        </SettingRow>
      </SettingGroup>
```

`src/pages/main/Live.tsx`의 `t("overlay.auto")`를 `t("translation.auto")`로 바꾼다.

- [ ] **Step 4: 검증**

Run: `mise exec -- yarn tsc --noEmit && mise exec -- yarn test && mise exec -- yarn build && grep -rn '"overlay.auto"\|overlay.subtitleLang\|overlay.sourceLang' src`
Expected: PASS, grep 결과 없음. (`onboarding.permission*` 참조는 Task 10이 온보딩을 다시 쓰면서 사라진다.)

- [ ] **Step 5: 커밋**

```bash
git add src/components/PermissionRow.tsx src/pages/settings/General.tsx src/pages/settings/Translation.tsx src/pages/main/Live.tsx
git commit -m "feat(settings): permission row with status icon, shortcuts tooltip, source/target language in translation tab"
```

---

### Task 9: 설정 탭 셸, 사이드바 A, 라우트

**Files:**
- Create: `src/pages/Settings.tsx`
- Modify: `src/components/Sidebar.tsx`, `src/pages/MainApp.tsx`, `src/pages/main/Live.tsx`, `src/pages/main/History.tsx`, `src/pages/settings/Models.tsx`, `vite.config.ts`, `src/vite-env.d.ts`

**Interfaces:**
- Produces: 라우트 `/settings/:tab`. `Sidebar({ collapsed, onToggle })` 시그니처 동일. `import.meta.env.PACKAGE_VERSION: string`.

- [ ] **Step 1: 버전 주입**

`vite.config.ts` 상단에 `import { readFileSync } from "node:fs";` 와
`const version = JSON.parse(readFileSync(new URL("./package.json", import.meta.url), "utf8")).version as string;`
를 추가하고 `defineConfig` 객체에 `define: { "import.meta.env.PACKAGE_VERSION": JSON.stringify(version) },`를 넣는다.

`src/vite-env.d.ts`에 추가:

```ts
interface ImportMetaEnv { readonly PACKAGE_VERSION: string }
interface ImportMeta { readonly env: ImportMetaEnv }
```

- [ ] **Step 2: Settings 셸**

`src/pages/Settings.tsx`:

```tsx
import { Navigate, NavLink, useParams } from "react-router";
import { useTranslation } from "react-i18next";
import { Icon } from "../components/icons";
import General from "./settings/General";
import Models from "./settings/Models";
import Translation from "./settings/Translation";
import Overlay from "./settings/Overlay";

const TABS = ["general", "models", "translation", "overlay"] as const;
type Tab = (typeof TABS)[number];
const isTab = (v: string | undefined): v is Tab => (TABS as readonly string[]).includes(v ?? "");

export default function Settings() {
  const { t } = useTranslation();
  const { tab } = useParams();
  if (!isTab(tab)) return <Navigate to="/settings/general" replace />;
  const body = { general: <General />, models: <Models />, translation: <Translation />, overlay: <Overlay /> }[tab];
  return (
    <div className="flex w-full max-w-3xl flex-col gap-4">
      <h2 className="text-2xl font-bold">{t("nav.settings")}</h2>
      <div role="tablist" className="tabs tabs-border">
        {TABS.map((k) => (
          <NavLink key={k} to={`/settings/${k}`} role="tab" className={({ isActive }) => `tab gap-1.5 ${isActive ? "tab-active" : ""}`}>
            <Icon name={k} />{t(`settings.${k}`)}
          </NavLink>
        ))}
      </div>
      {body}
    </div>
  );
}
```

`src/pages/settings/Models.tsx`: `<h2>` 줄을 지우고 최상위 `div`를 `flex flex-col gap-4`로. `onSelect`는 그대로.

- [ ] **Step 3: Sidebar 교체**

`src/components/Sidebar.tsx` 전체:

```tsx
import { useRef, useState } from "react";
import { NavLink, useNavigate } from "react-router";
import { useTranslation } from "react-i18next";
import { Icon, type IconName } from "./icons";
import { useModels } from "../lib/models";
import { useSession } from "../lib/session";
import { useSettings } from "../lib/settings";
import { api } from "../lib/tauri";
import type { HwInfo } from "../lib/types";

interface Item { to: string; icon: IconName; label: string }

export function Sidebar({ collapsed, onToggle }: { collapsed: boolean; onToggle: () => void }) {
  const { t } = useTranslation();
  const navigate = useNavigate();
  const { view, start, stop } = useSession();
  const modelId = useSettings((s) => s.settings?.asr.model_id);
  const asrInstalled = useModels((s) => s.models.some((m) => m.info.id === modelId && m.installed));
  const [q, setQ] = useState("");
  const [hw, setHw] = useState<HwInfo | null>(null);
  const about = useRef<HTMLDialogElement>(null);

  const items: Item[] = [
    { to: "/live", icon: "live", label: t("nav.live") },
    { to: "/history", icon: "history", label: t("nav.history") },
  ];
  // 수동 접힘은 항상 숨긴다. 자동은 800px 미만에서만.
  const label = collapsed ? "hidden" : "hidden wide:inline";
  const wide = collapsed ? "" : "wide:w-52";
  const start = collapsed ? "" : "wide:justify-start";
  const navCls = ({ isActive }: { isActive: boolean }) =>
    `btn btn-sm justify-center gap-2 ${start} ${isActive ? "btn-neutral" : "btn-ghost text-fg-muted"}`;
  const openAbout = () => { api.getHwInfo().then(setHw).catch(() => {}); about.current?.showModal(); };
  const hwLine = hw ? [hw.chip, `${hw.mem_gb} GB`, hw.gpu && `${hw.gpu}${hw.gpu_mem_gb ? ` ${hw.gpu_mem_gb} GB` : ""}`].filter(Boolean).join(" · ") : "";

  return (
    <aside className={`relative flex w-14 shrink-0 flex-col gap-1 border-r border-base-300 bg-base-200 p-2 ${wide}`}>
      <div className={`mb-1 flex items-center justify-center gap-2 px-1 pt-1 font-bold ${start}`}>
        <span className="h-5 w-5 shrink-0 rounded-md bg-primary" />
        <span className={label}>{t("app.name")}</span>
      </div>
      {!collapsed && (
        <input
          value={q}
          onChange={(e) => setQ(e.target.value)}
          onKeyDown={(e) => { if (e.key === "Enter" && q.trim()) { navigate(`/history?q=${encodeURIComponent(q.trim())}`); setQ(""); } }}
          placeholder={t("nav.search")}
          aria-label={t("nav.search")}
          className="input input-sm mb-1 hidden w-full rounded-full wide:block"
        />
      )}
      {items.map((i) => (
        <NavLink key={i.to} to={i.to} className={navCls} aria-label={i.label}>
          <Icon name={i.icon} /><span className={label}>{i.label}</span>
        </NavLink>
      ))}
      <button
        type="button"
        onClick={onToggle}
        aria-label={collapsed ? t("nav.expand") : t("nav.collapse")}
        className="btn btn-circle btn-neutral btn-xs absolute -right-3 top-12 hidden wide:flex"
      >
        <Icon name="collapse" className={`h-3 w-3 ${collapsed ? "rotate-180" : ""}`} />
      </button>

      <div className="mt-auto flex flex-col gap-1">
        <button
          type="button"
          className="btn btn-primary btn-sm btn-block gap-1"
          disabled={view.stopping || (!view.capturing && !asrInstalled)}
          title={!view.capturing && !asrInstalled ? t("errors.modelMissing") : undefined}
          onClick={() => (view.capturing ? stop() : start())}
        >
          <span>{view.capturing ? "■" : "●"}</span>
          <span className={label}>{view.stopping ? t("live.stopping") : view.capturing ? t("live.stop") : t("live.start")}</span>
        </button>
        <NavLink to="/settings/general" className={navCls} aria-label={t("nav.settings")}>
          <Icon name="general" /><span className={label}>{t("nav.settings")}</span>
        </NavLink>
        <button type="button" className={`btn btn-ghost btn-sm justify-center gap-2 text-fg-muted ${start}`} onClick={openAbout} aria-label={t("nav.about")}>
          <Icon name="info" /><span className={label}>{t("nav.about")}</span>
        </button>
        <div className={`text-center text-[10px] text-fg-muted ${label}`}>v{import.meta.env.PACKAGE_VERSION}</div>
      </div>

      <dialog ref={about} className="modal">
        <div className="modal-box max-w-sm">
          <h3 className="text-lg font-bold">{t("app.name")}</h3>
          <div className="mt-2 text-sm"><span className="text-fg-muted">{t("about.version")}</span> {import.meta.env.PACKAGE_VERSION}</div>
          {hwLine && <div className="text-sm"><span className="text-fg-muted">{t("about.hardware")}</span> {hwLine}</div>}
          <div className="modal-action">
            <form method="dialog"><button className="btn btn-sm">{t("common.close")}</button></form>
          </div>
        </div>
        <form method="dialog" className="modal-backdrop"><button aria-label={t("common.close")} /></form>
      </dialog>
    </aside>
  );
}
```

- [ ] **Step 4: MainApp 라우트**

`src/pages/MainApp.tsx`의 import에서 `General, Models, Translation, Overlay`를 지우고 `import Settings from "./Settings";`를 추가. `<Routes>`를 교체:

```tsx
          <Routes>
            <Route path="/" element={<Navigate to="/live" replace />} />
            <Route path="/live" element={<Live />} />
            <Route path="/history" element={<History />} />
            <Route path="/settings" element={<Navigate to="/settings/general" replace />} />
            <Route path="/settings/transcription" element={<Navigate to="/settings/models" replace />} />
            <Route path="/settings/:tab" element={<Settings />} />
            <Route path="*" element={<Navigate to="/live" replace />} />
          </Routes>
```

`<div className="flex h-full bg-base">` → `bg-base-100`.

- [ ] **Step 5: 라이브 헤더와 히스토리 검색 파라미터**

`src/pages/main/Live.tsx`: `start, stop`를 `useSession()`에서 더 이상 꺼내지 않는다(`const { view } = useSession();`). 헤더 버튼 블록을 교체:

```tsx
        <div className="join">
          <button type="button" className={`btn btn-sm join-item ${settings.overlay.enabled ? "btn-primary" : "btn-ghost"}`} onClick={() => update({ overlay: { enabled: true } })}>{t("live.overlay")} {t("live.overlayOn")}</button>
          <button type="button" className={`btn btn-sm join-item ${settings.overlay.enabled ? "btn-ghost" : "btn-primary"}`} onClick={() => update({ overlay: { enabled: false } })}>{t("live.overlayOff")}</button>
        </div>
```

`src/pages/main/History.tsx`: `import { useSearchParams } from "react-router";` 추가. `const [q, setQ] = useState("");`를 다음으로 교체:

```tsx
  const [params] = useSearchParams();
  const [q, setQ] = useState(params.get("q") ?? "");
  // 사이드바 검색은 ?q= 로 들어온다. 파라미터가 바뀌면 검색어도 따라간다.
  useEffect(() => { const v = params.get("q"); if (v !== null) setQ(v); }, [params]);
```

- [ ] **Step 6: 검증**

Run: `mise exec -- yarn tsc --noEmit && mise exec -- yarn test && mise exec -- yarn build`
Expected: PASS.

- [ ] **Step 7: 앱에서 확인**

Run: `mise exec -- yarn tauri dev` (온보딩이 끝난 설정이 있어야 메인 창이 뜬다). 확인: 사이드바 하단 시작 버튼, 설정 탭 4개 전환, 정보 모달, 창을 800px 미만으로 줄이면 아이콘만 남음, 검색 Enter 시 히스토리로 이동. 종료 후 다음 단계.

- [ ] **Step 8: 커밋**

```bash
git add vite.config.ts src/vite-env.d.ts src/pages/Settings.tsx src/components/Sidebar.tsx src/pages/MainApp.tsx src/pages/main/Live.tsx src/pages/main/History.tsx src/pages/settings/Models.tsx
git commit -m "feat(ui): fixed sidebar with bottom actions, settings tabs, responsive collapse"
```

---

### Task 10: 온보딩 — steps, 백그라운드 다운로드, 체크리스트

**Files:**
- Modify: `src/pages/Onboarding.tsx`

**Interfaces:**
- Consumes: `useModels.enqueue`, `DownloadToast`, `PermissionRow`, `ModelRow`.

- [ ] **Step 1: Onboarding 재작성**

`src/pages/Onboarding.tsx` 전체:

```tsx
import { useEffect, useState } from "react";
import { useTranslation } from "react-i18next";
import { DownloadToast } from "../components/DownloadToast";
import { ErrorBar } from "../components/ErrorBar";
import { Icon } from "../components/icons";
import { ModelRow } from "../components/ModelRow";
import { PermissionIcon, PermissionRow, type Perm } from "../components/PermissionRow";
import { SettingGroup } from "../components/SettingGroup";
import { useModels } from "../lib/models";
import { api } from "../lib/tauri";
import { useSettings } from "../lib/settings";
import type { ModelKind, UiLang } from "../lib/types";

type Step = "language" | "permission" | "asr" | "llm" | "done";
const ALL: Step[] = ["language", "permission", "asr", "llm", "done"];

export default function Onboarding() {
  const { t } = useTranslation();
  const { settings, update, setError } = useSettings();
  const { models, enqueue, refresh } = useModels();
  const [steps, setSteps] = useState<Step[]>(ALL);
  const [idx, setIdx] = useState(0);
  const [skippedLlm, setSkippedLlm] = useState(false);
  // 권한 단계가 조회한 값. 완료 단계에서 다시 탭을 만들지 않는다.
  const [perm, setPerm] = useState<Perm | null>(null);

  useEffect(() => {
    api.getPlatform().then((p) => { if (p !== "macos") setSteps((s) => s.filter((x) => x !== "permission")); }).catch(() => {});
    refresh();
  }, []);

  const last = steps.length - 1;
  const cur = Math.min(idx, last);
  const step = steps[cur];
  const next = () => setIdx(Math.min(cur + 1, last));
  const back = () => setIdx(Math.max(cur - 1, 0));

  if (!settings) return null;
  const byId = (id: string) => models.find((m) => m.info.id === id);
  const asr = byId(settings.asr.model_id);
  const llm = byId(settings.translation.local_model);
  const macos = steps.includes("permission");

  // 모델 단계의 "다음": 미설치면 뒤에서 받기 시작하고 바로 넘어간다.
  const nextFromModel = (kind: ModelKind) => {
    const chosen = kind === "asr" ? asr : llm;
    if (chosen && !chosen.installed && !chosen.download) enqueue(chosen.info.id);
    if (kind === "llm") setSkippedLlm(false);
    next();
  };
  const select = (kind: ModelKind, id: string) =>
    (kind === "asr" ? update({ asr: { model_id: id } }) : update({ translation: { local_model: id } })).then(() => refresh());
  const rows = (kind: ModelKind) => {
    const current = kind === "asr" ? settings.asr.model_id : settings.translation.local_model;
    return models.filter((m) => m.info.kind === kind).map((m) => (
      <ModelRow key={m.info.id} status={m} selected={current === m.info.id} onSelect={() => select(kind, m.info.id)} />
    ));
  };
  const langBtn = (v: UiLang, text: string) => (
    <button key={v} type="button" className={`btn btn-sm ${settings.general.ui_language === v ? "btn-primary" : "btn-neutral"}`} onClick={() => update({ general: { ui_language: v } })}>{text}</button>
  );
  const pct = (m: typeof asr) => (m?.download ? `${Math.round((m.download.received / Math.max(1, m.download.total)) * 100)}%` : null);
  const mark = (ok: boolean, progress: string | null) => ok
    ? <span className="flex h-6 w-6 items-center justify-center rounded-full bg-primary text-primary-content"><Icon name="check" /></span>
    : progress
      ? <span className="text-xs tabular-nums text-fg-muted">{progress}</span>
      : <span className="flex h-6 w-6 items-center justify-center rounded-full bg-error text-error-content"><Icon name="x" /></span>;

  return (
    <div className="flex h-full flex-col gap-4 p-6">
      <DownloadToast />
      <ul className="steps w-full text-xs">
        {steps.map((s, i) => (
          <li key={s} className={`step ${i <= cur ? "step-primary" : ""}`} data-content={i < cur ? "✓" : String(i + 1)}>{t(`onboarding.step.${s}`)}</li>
        ))}
      </ul>
      <ErrorBar />
      <div className="mx-auto flex w-full max-w-2xl flex-1 flex-col gap-4 overflow-hidden">
        <h2 className="text-2xl font-bold">{t(`onboarding.title.${step}`)}</h2>

        <div className="flex flex-1 flex-col gap-2 overflow-auto">
          {step === "language" && <div className="flex flex-wrap gap-2">{langBtn("system", t("general.langSystem"))}{langBtn("ko", "한국어")}{langBtn("en", "English")}{langBtn("ja", "日本語")}</div>}
          {step === "permission" && <PermissionRow onStatus={setPerm} />}
          {step === "asr" && rows("asr")}
          {step === "llm" && rows("llm")}
          {step === "done" && (
            <SettingGroup>
              <div className="flex items-center justify-between px-4 py-3 text-sm"><span>{t("onboarding.check.asr")} · {asr?.info.name ?? "—"}</span>{mark(Boolean(asr?.installed), pct(asr))}</div>
              <div className="flex items-center justify-between px-4 py-3 text-sm">
                <span>{t("onboarding.check.llm")} · {skippedLlm ? t("onboarding.skipped") : llm?.info.name ?? "—"}</span>
                {skippedLlm ? <span className="text-xs text-fg-muted">—</span> : mark(Boolean(llm?.installed), pct(llm))}
              </div>
              {macos && <div className="flex items-center justify-between px-4 py-3 text-sm"><span>{t("permission.name")}</span><PermissionIcon perm={perm} /></div>}
            </SettingGroup>
          )}
        </div>

        <div className="flex items-center justify-between">
          <button type="button" className="btn btn-ghost btn-sm" onClick={back} disabled={cur === 0}>{t("onboarding.back")}</button>
          <div className="flex gap-2">
            {step === "llm" && <button type="button" className="btn btn-outline btn-sm" onClick={() => { setSkippedLlm(true); next(); }}>{t("onboarding.skip")}</button>}
            {(step === "language" || step === "permission") && <button type="button" className="btn btn-primary btn-sm" onClick={next}>{t("onboarding.next")}</button>}
            {step === "asr" && <button type="button" className="btn btn-primary btn-sm" disabled={!asr} onClick={() => nextFromModel("asr")}>{t("onboarding.next")}</button>}
            {step === "llm" && <button type="button" className="btn btn-primary btn-sm" disabled={!llm} onClick={() => nextFromModel("llm")}>{t("onboarding.next")}</button>}
            {step === "done" && <button type="button" className="btn btn-primary btn-sm" disabled={!asr?.installed} onClick={() => api.finishOnboarding().catch(setError)}>{t("onboarding.finish")}</button>}
          </div>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 검증**

Run: `mise exec -- yarn tsc --noEmit && mise exec -- yarn test && mise exec -- yarn build && grep -rn 'models.continue\|onboarding.permission[A-Z]\|onboarding.openSettings' src`
Expected: PASS, grep 결과 없음.

- [ ] **Step 3: 앱에서 확인**

설정 파일의 `general.onboarding_done`을 `false`로 바꾸고(`~/Library/Application Support/com.babelay.app/settings.json`) `mise exec -- yarn tauri dev`. 확인: steps 헤더, 권한 행 체크 아이콘, Transcript 단계에서 미설치 모델 선택 후 다음 → 오른쪽 상단 토스트, 번역 단계에서 다음 → 토스트에 "다음: …", 완료 단계 체크리스트, Transcript 설치 전 시작하기 비활성, 창 리사이즈. 종료.

- [ ] **Step 4: 커밋**

```bash
git add src/pages/Onboarding.tsx
git commit -m "feat(onboarding): steps header, background downloads with queue, readiness checklist"
```

---

### Task 11: 마무리 — 잔여 검증, 수동 체크리스트, 문서

**Files:**
- Modify: `docs/superpowers/specs/2026-09-03-phase2.5-ui-onboarding-design.md`(상태), `docs/superpowers/specs/2026-09-02-babelay-design.md`(§2 표, §7, §11)

- [ ] **Step 1: 전체 게이트**

```bash
cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --all -- --check
mise exec -- yarn tsc --noEmit && mise exec -- yarn test && mise exec -- yarn build
grep -rn "전사" src/locales   # 결과 없음이어야 한다
```

- [ ] **Step 2: 수동 체크리스트(macOS, `mise exec -- yarn tauri dev`)**

- [ ] 다크/라이트/시스템 전환: 사이드바·카드·버튼 색이 토큰대로, 초록은 채우기+검정 글자만.
- [ ] 메인 창을 720px까지 줄여도 가로 스크롤 없음, 사이드바 아이콘만, 모델 행 버튼이 줄바꿈.
- [ ] 설정 › 모델: 사용 중 모델 삭제 → 확인 모달 → 행 배지 사라짐, 사이드바 시작 버튼 disabled + 툴팁.
- [ ] 캡처 중 삭제 시도 → "캡처 중에는 모델을 삭제할 수 없습니다".
- [ ] 설정 › 오버레이: 표시 모드 전환이 미리보기와 실제 오버레이(위치 조정 켠 상태)에 동시에 반영. 번역만 → 빈 상자.
- [ ] 위치 조정: 예시 문구가 오버레이 창에 보임, 드래그 이동, 모서리 드래그로 폭 변경, 조정 완료 후 같은 폭으로 복원(앱 재시작 후에도).
- [ ] 온보딩 전체 흐름(Task 10 Step 3) 재확인. 다운로드 도중 취소 → 큐의 다음 항목이 이어서 시작.
- [ ] 설정 › 일반: 권한 행 체크 아이콘, `?` 툴팁에 단축키, 언어 select의 "시스템 기본 (한국어)".

- [ ] **Step 3: 문서 갱신**

`docs/superpowers/specs/2026-09-03-phase2.5-ui-onboarding-design.md`의 `상태: 사용자 검토 대기` → `상태: 구현 완료(2026-09-03)`.

`docs/superpowers/specs/2026-09-02-babelay-design.md`:
- §2 표에 행 추가: `| 메인 창 구조(2.5) | 고정 사이드바 + 하단 시작·설정·정보, 설정은 탭 4개, daisyUI 5 (2026-09-03 스펙 참조) |`
- §7 첫 줄 "UI 라이브러리 없음" → "daisyUI 5(CSS 플러그인)만 사용, 아이콘 라이브러리 없음".
- §11에 항목 추가: `2.5. **UI 리디자인 + 백그라운드 온보딩 + 오버레이 수정**: 스펙 docs/superpowers/specs/2026-09-03-phase2.5-ui-onboarding-design.md — 완료(2026-09-03)`.

- [ ] **Step 4: 커밋**

```bash
git add docs/superpowers/specs
git commit -m "docs: phase 2.5 complete; design spec reflects daisyUI and sidebar structure"
```
