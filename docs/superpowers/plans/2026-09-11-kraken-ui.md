# Kraken UI 적용 · 상단 바 재배치 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 앱 표현층 전체를 Kraken 디자인 시스템(퍼플, 흰 표면, 12px 라운드)으로 바꾸고, 사이드바를 상단 바로 교체해 시작/정지·언어·오버레이 컨트롤을 라이브 화면 한 곳에 모은다.

**Architecture:** daisyUI 테마 토큰 두 벌(`babelay` 다크, `babelay-light`)의 값만 교체해 색·라운드가 앱 전체에 퍼지게 하고, 컴포넌트는 클래스 문자열만 바꾼다. `Sidebar` 를 `TopBar` 로 대체하고 About 대화상자는 `AboutDialog` 로 분리해 설정 › 일반으로 옮긴다. Rust 백엔드·설정 스키마·라우트·스토어는 손대지 않는다.

**Tech Stack:** React 19, react-router 7, Tailwind CSS 4, daisyUI 5.7, react-i18next, Vite 7, vitest 4, Tauri 2, Pretendard 1.3.9(npm `pretendard`).

**Spec:** `docs/superpowers/specs/2026-09-11-kraken-ui-design.md`

## Global Constraints

- 테마 이름 `babelay`(다크, default)와 `babelay-light` 는 그대로. `src/lib/theme.ts` 와 `src/test/theme-dom.test.ts` 는 수정하지 않는다.
- 라운드: `--radius-box: 12px; --radius-field: 12px; --radius-selector: 6px`. `rounded-full` 은 입력·버튼에 쓰지 않는다(`btn-circle`, 상태 점은 예외).
- 그림자는 `shadow-kr` 하나(`rgba(0,0,0,.03) 0 4px 24px`).
- 글꼴 스택: `"Pretendard Variable", Pretendard, -apple-system, "Helvetica Neue", Arial, "Hiragino Sans", "Meiryo", sans-serif`. 본문 14px.
- 세 로케일(`ko`, `en`, `ja`)의 키 집합은 항상 같아야 한다(`src/test/locales.test.ts`).
- 캡처 중 시작 버튼: `aura aura-rainbow` 래퍼 + `btn-neutral` + `.eq-bars text-primary`. 정지 상태: `btn-primary` + play 아이콘.
- 커밋 메시지는 한국어 요약 + 아래 두 줄로 끝낸다:
  ```
  Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01U9PLCfTJosJ5x85Rk936CS
  ```
- 검증 명령: `yarn test`(vitest), `yarn build`(tsc + vite). 두 명령이 모두 통과해야 태스크가 끝난다.
- 스펙과 다른 결정 두 가지(이 계획이 우선): (1) 설정 › 일반의 정보 행은 별도 섹션 제목 없이 `SettingGroup` 한 개, 라벨 `app.name`, 버튼 `nav.about`. (2) 다크 테마 `error-content` 는 `#101114`(흰 글자는 `#f3727f` 위에서 대비가 낮다).

---

## 파일 구조

| 파일 | 책임 |
|---|---|
| `src/index.css` | 테마 토큰 두 벌, 글꼴, 그림자, `.eq-bars` |
| `src/components/TopBar.tsx` (신설) | 로고 · 탭 · 검색 · 시작/정지. `Sidebar.tsx` 의 세션 버튼 로직을 승계 |
| `src/components/AboutDialog.tsx` (신설) | "정보" 버튼 + `<dialog>`. `Sidebar.tsx` 의 About 을 승계 |
| `src/components/Sidebar.tsx` | 삭제 |
| `src/pages/MainApp.tsx` | 세로 뼈대(TopBar + main) |
| `src/pages/main/Live.tsx` | 컨트롤 줄(언어 select · 배지 · 오버레이 토글) + 자막 카드 |
| `src/pages/main/History.tsx` | 제목 제거, 카드 스타일 |
| `src/pages/Settings.tsx`, `src/pages/settings/General.tsx` | 제목 제거, 정보 행 |
| `src/components/SettingGroup.tsx`, `ModelPicker.tsx`, `ModelRow.tsx`, `PermissionRow.tsx` | 클래스 교체 |
| `src/pages/Onboarding.tsx` | 클래스 교체 |
| `src/locales/*.json` | 안 쓰는 키 삭제 |
| `assets/icon.svg`, `src-tauri/icons/*` | 아이콘 |

---

### Task 1: 디자인 토큰과 글꼴

**Files:**
- Modify: `package.json`
- Modify: `src/index.css`

**Interfaces:**
- Produces: Tailwind 유틸리티 `shadow-kr`, `text-fg-muted`/`bg-fg-muted`, daisyUI 색 토큰(`primary`, `secondary`, `success`, `warning`, `error`, `neutral`, `base-100/200/300`). 이후 모든 태스크가 이 클래스를 쓴다.
- `wide:` 브레이크포인트는 사라진다(Task 3 에서 마지막 사용처인 `Sidebar.tsx` 를 지운다). Task 1 시점에는 `Sidebar.tsx` 가 `wide:` 를 쓰지만 Tailwind 는 모르는 variant 를 무시하므로 빌드는 깨지지 않는다.

- [ ] **Step 1: Pretendard 설치**

Run: `yarn add -D pretendard@1.3.9`
Expected: `package.json` devDependencies 에 `"pretendard": "1.3.9"` 추가, `node_modules/pretendard/dist/web/variable/pretendardvariable.css` 존재.

- [ ] **Step 2: `src/index.css` 의 테마·글꼴 블록 교체**

파일 맨 위부터 `@theme { … }` 와 `[data-theme="babelay-light"] { … }` 까지를 아래로 바꾼다. 그 아래 `html, body, #root` 부터 `.eq-bars` 까지는 그대로 둔다.

```css
@import "tailwindcss";
@import "pretendard/dist/web/variable/pretendardvariable.css";
@source not "../docs";
@plugin "daisyui" { themes: false; exclude: rootcolor; }
@plugin "daisyui/theme" {
  name: "babelay"; default: true; color-scheme: dark;
  --color-base-100: #101114; --color-base-200: #16171c; --color-base-300: #2a2b34; --color-base-content: #f4f4f7;
  --color-neutral: #1c1d24; --color-neutral-content: #f4f4f7;
  --color-primary: #855bfb; --color-primary-content: #ffffff;
  --color-secondary: #2a2350; --color-secondary-content: #b49bff;
  --color-accent: #855bfb; --color-accent-content: #ffffff;
  --color-info: #9b7cff; --color-info-content: #101114;
  --color-success: #4cd393; --color-success-content: #101114;
  --color-warning: #fbbf24; --color-warning-content: #101114;
  --color-error: #f3727f; --color-error-content: #101114;
  --radius-box: 12px; --radius-field: 12px; --radius-selector: 6px;
  --size-field: 0.25rem; --size-selector: 0.25rem; --border: 1px; --depth: 0; --noise: 0;
}
@plugin "daisyui/theme" {
  name: "babelay-light"; color-scheme: light;
  --color-base-100: #ffffff; --color-base-200: #f6f6f9; --color-base-300: #dedee5; --color-base-content: #101114;
  --color-neutral: #f6f6f9; --color-neutral-content: #101114;
  --color-primary: #7132f5; --color-primary-content: #ffffff;
  --color-secondary: #ede7fe; --color-secondary-content: #7132f5;
  --color-accent: #7132f5; --color-accent-content: #ffffff;
  --color-info: #5741d8; --color-info-content: #ffffff;
  --color-success: #149e61; --color-success-content: #ffffff;
  --color-warning: #f59e0b; --color-warning-content: #101114;
  --color-error: #d33a4a; --color-error-content: #ffffff;
  --radius-box: 12px; --radius-field: 12px; --radius-selector: 6px;
  --size-field: 0.25rem; --size-selector: 0.25rem; --border: 1px; --depth: 0; --noise: 0;
}

/* react-toastify 는 theme="light" 로 고정하고 색만 daisyUI 변수에 붙인다 → 다크/라이트를 자동으로 따라간다.
   라이브러리 CSS 는 런타임에 <head> 뒤쪽으로 주입되므로 :root 하나로는 진다. :root:root 으로 특이도를 올린다. */
:root:root {
  --toastify-color-light: var(--color-neutral);
  --toastify-text-color-light: var(--color-neutral-content);
  --toastify-color-info: var(--color-info);
  --toastify-color-success: var(--color-success);
  --toastify-color-warning: var(--color-warning);
  --toastify-color-error: var(--color-error);
  --toastify-color-progress-light: var(--color-primary);
  --toastify-toast-bd-radius: var(--radius-box);
  --toastify-font-family: inherit;
}
/* 닫기 ✕ 는 color 가 변수가 아니라 #000 하드코딩이다. 다크에서 안 보이므로 여기서 덮는다. */
:root:root .Toastify__close-button--light { color: var(--color-neutral-content); opacity: .7; }

@theme {
  --color-fg-muted: #9497a9;
  --shadow-kr: rgba(0, 0, 0, 0.03) 0px 4px 24px;
  --font-sans: "Pretendard Variable", Pretendard, -apple-system, "Helvetica Neue", Arial, "Hiragino Sans", "Meiryo", sans-serif;
}
```

지워지는 것: `--breakpoint-wide: 800px;`, `[data-theme="babelay-light"] { --color-fg-muted: #6a6a6a; }`(양쪽 같은 값이라 불필요).

- [ ] **Step 3: 검증**

Run: `yarn test && yarn build`
Expected: vitest 전부 PASS(특히 `theme-dom.test.ts`), `dist/assets/` 에 `PretendardVariable-*.woff2` 가 생김.

Run: `ls dist/assets | grep -i pretendard`
Expected: woff2 파일 1개.

- [ ] **Step 4: 커밋**

```bash
git add package.json yarn.lock src/index.css
git commit -m "feat(ui): Kraken 디자인 토큰과 Pretendard 글꼴

daisyUI 테마 두 벌의 색·라운드를 Kraken 팔레트(퍼플 #7132f5, 흰 표면, 12px)로
바꾸고 Pretendard Variable 을 번들한다. shadow-kr 유틸리티 추가.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01U9PLCfTJosJ5x85Rk936CS"
```

---

### Task 2: AboutDialog 분리와 설정 › 일반 정보 행

**Files:**
- Create: `src/components/AboutDialog.tsx`
- Modify: `src/pages/settings/General.tsx`
- Modify: `src/locales/ko.json`, `src/locales/en.json`, `src/locales/ja.json` (`about.hardware` 삭제)

**Interfaces:**
- Produces: `export function AboutDialog(): JSX.Element` — "정보" 버튼과 `<dialog>` 를 함께 렌더링한다. props 없음. Task 3 에서 `Sidebar.tsx` 를 지울 때 About 코드가 이미 여기로 옮겨져 있어야 한다.

- [ ] **Step 1: `src/components/AboutDialog.tsx` 작성**

```tsx
import { useEffect, useRef, useState } from "react";
import { useTranslation } from "react-i18next";
import { api } from "../lib/tauri";
import type { HwInfo } from "../lib/types";
import logo from "../../assets/icon.svg";

// "정보" 버튼 + macOS "이 Mac에 관하여" 식 대화상자. 설정 › 일반에서만 쓴다.
export function AboutDialog() {
  const { t } = useTranslation();
  const ref = useRef<HTMLDialogElement>(null);
  const [hw, setHw] = useState<HwInfo | null>(null);
  // 설정 페이지에 들어올 때 한 번만 조회한다. 열 때마다 IPC 를 부르지 않는다.
  useEffect(() => { api.getHwInfo().then(setHw).catch(() => {}); }, []);

  return (
    <>
      <button type="button" className="btn btn-sm" onClick={() => ref.current?.showModal()}>{t("nav.about")}</button>
      <dialog ref={ref} className="modal">
        <div className="modal-box max-w-xs">
          <div className="flex flex-col items-center gap-1 text-center">
            <img src={logo} alt="" className="h-16 w-16" />
            <h3 className="mt-2 text-lg font-bold tracking-tight">{t("app.name")}</h3>
            <div className="text-xs text-fg-muted">{t("about.version")} {import.meta.env.PACKAGE_VERSION}</div>
          </div>
          {hw && (
            <dl className="mx-auto mt-5 grid w-fit grid-cols-[auto_auto] gap-x-4 gap-y-1 text-sm">
              <dt className="text-right text-fg-muted">{t("about.chip")}</dt><dd>{hw.chip}</dd>
              <dt className="text-right text-fg-muted">{t("about.memory")}</dt><dd>{hw.mem_gb} GB</dd>
              {hw.gpu && <><dt className="text-right text-fg-muted">{t("about.graphics")}</dt><dd>{hw.gpu}{hw.gpu_mem_gb ? ` · ${hw.gpu_mem_gb} GB` : ""}</dd></>}
            </dl>
          )}
          <div className="modal-action justify-center">
            <form method="dialog"><button className="btn btn-sm">{t("common.close")}</button></form>
          </div>
        </div>
        <form method="dialog" className="modal-backdrop"><button aria-label={t("common.close")} /></form>
      </dialog>
    </>
  );
}
```

- [ ] **Step 2: `General.tsx` 에 정보 행 추가**

import 에 한 줄 추가:

```tsx
import { AboutDialog } from "../../components/AboutDialog";
```

return 의 맨 마지막, `{platform === "macos" && (…)}` 블록 **뒤**(바깥 `<div className="flex flex-col gap-4">` 가 닫히기 전)에 추가:

```tsx
      <SettingGroup>
        <SettingRow as="div" label={t("app.name")}>
          <AboutDialog />
        </SettingRow>
      </SettingGroup>
```

- [ ] **Step 3: 안 쓰는 로케일 키 삭제**

세 파일의 `"about"` 줄에서 `"hardware": "…", ` 항목을 지운다. 결과:

```json
"about": { "version": "버전", "chip": "칩", "memory": "메모리", "graphics": "그래픽" },
```
```json
"about": { "version": "Version", "chip": "Chip", "memory": "Memory", "graphics": "Graphics" },
```
```json
"about": { "version": "バージョン", "chip": "チップ", "memory": "メモリ", "graphics": "グラフィックス" },
```

- [ ] **Step 4: 검증**

Run: `grep -rn "about.hardware" src; yarn test && yarn build`
Expected: grep 결과 없음. 테스트·빌드 PASS.

- [ ] **Step 5: 커밋**

```bash
git add src/components/AboutDialog.tsx src/pages/settings/General.tsx src/locales
git commit -m "feat(ui): 정보 대화상자를 설정 › 일반으로

사이드바가 없어질 준비. About 을 AboutDialog 컴포넌트로 분리해 일반 탭
맨 아래 행에 둔다. 안 쓰는 about.hardware 키 삭제.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01U9PLCfTJosJ5x85Rk936CS"
```

---

### Task 3: TopBar 신설, Sidebar 삭제, MainApp 뼈대

**Files:**
- Create: `src/components/TopBar.tsx`
- Modify: `src/pages/MainApp.tsx`
- Delete: `src/components/Sidebar.tsx`
- Modify: `src/locales/ko.json`, `en.json`, `ja.json` (`nav.collapse`, `nav.expand` 삭제)

**Interfaces:**
- Consumes: `useSession()` → `{ view: { capturing, stopping, … }, start(), stop() }`, `useModels`, `useSettings`, `useUpdate` (모두 기존, 시그니처 변경 없음).
- Produces: `export function TopBar(): JSX.Element` — props 없음. `HashRouter` 안에서만 렌더링(내부에서 `useNavigate`, `NavLink` 사용).

- [ ] **Step 1: `src/components/TopBar.tsx` 작성**

```tsx
import { useState } from "react";
import { NavLink, useNavigate } from "react-router";
import { useTranslation } from "react-i18next";
import { Icon } from "./icons";
import { useModels } from "../lib/models";
import { useSession } from "../lib/session";
import { useSettings } from "../lib/settings";
import { useUpdate } from "../lib/update";
import logo from "../../assets/icon.svg";

export function TopBar() {
  const { t } = useTranslation();
  const navigate = useNavigate();
  const { view, start, stop } = useSession();
  const modelId = useSettings((s) => s.settings?.asr.model_id);
  const asrInstalled = useModels((s) => s.models.some((m) => m.info.id === modelId && m.installed));
  const pending = useUpdate((s) => s.info !== null && s.progress === null);
  const [q, setQ] = useState("");

  const missing = !view.capturing && !asrInstalled;
  const label = view.stopping ? t("live.stopping") : view.capturing ? t("live.stop") : t("live.start");
  const tab = ({ isActive }: { isActive: boolean }) =>
    `btn btn-sm btn-ghost gap-1.5 ${isActive ? "bg-secondary text-secondary-content" : "text-fg-muted"}`;

  return (
    <header className="flex h-12 shrink-0 items-center gap-3 border-b border-base-300 px-4">
      <img src={logo} alt="" className="h-6 w-6 shrink-0" />
      <nav className="flex gap-0.5">
        <NavLink to="/live" className={tab}><Icon name="live" />{t("nav.live")}</NavLink>
        <NavLink to="/history" className={tab}><Icon name="history" />{t("nav.history")}</NavLink>
        <NavLink to="/settings/general" className={tab}>
          <Icon name="general" />{t("nav.settings")}
          {pending && <span role="status" className="h-1.5 w-1.5 rounded-full bg-primary" aria-label={t("update.pending")} />}
        </NavLink>
      </nav>
      <div className="flex-1" />
      <input
        value={q}
        onChange={(e) => setQ(e.target.value)}
        onKeyDown={(e) => { if (e.key === "Enter" && q.trim()) { navigate(`/history?q=${encodeURIComponent(q.trim())}`); setQ(""); } }}
        placeholder={t("nav.search")}
        aria-label={t("nav.search")}
        className="input input-sm w-44"
      />
      <div className={missing ? "tooltip tooltip-bottom" : ""} data-tip={missing ? t("errors.modelMissing") : undefined}>
        {/* 캡처 중: 표면색 버튼 + 무지개 링 + 파형. 정지 중에는 링을 끄고 파형을 멈춘다. */}
        <div className={view.capturing && !view.stopping ? "aura aura-rainbow block" : ""}>
          <button
            type="button"
            className={`btn btn-sm gap-1.5 ${view.capturing ? "btn-neutral" : "btn-primary"}`}
            disabled={view.stopping || missing}
            aria-label={label}
            onClick={() => (view.capturing ? stop() : start())}
          >
            {view.capturing
              ? <span aria-hidden="true" className={`eq-bars text-primary ${view.stopping ? "paused" : ""}`}><i /><i /><i /><i /></span>
              : <Icon name="play" className="h-3.5 w-3.5 fill-current" />}
            {label}
          </button>
        </div>
      </div>
    </header>
  );
}
```

- [ ] **Step 2: `src/pages/MainApp.tsx` 전체 교체**

```tsx
import { useEffect } from "react";
import { HashRouter, Navigate, Route, Routes } from "react-router";
import { TopBar } from "../components/TopBar";
import Live from "./main/Live";
import History from "./main/History";
import Settings from "./Settings";
import { useUpdate } from "../lib/update";

export default function MainApp() {
  useEffect(() => useUpdate.getState().subscribe(), []);

  return (
    <HashRouter>
      <div className="flex h-full flex-col bg-base-100">
        <TopBar />
        <main className="min-h-0 flex-1 overflow-auto px-6 py-5">
          <Routes>
            <Route path="/" element={<Navigate to="/live" replace />} />
            <Route path="/live" element={<Live />} />
            <Route path="/history" element={<History />} />
            <Route path="/settings" element={<Navigate to="/settings/general" replace />} />
            <Route path="/settings/transcription" element={<Navigate to="/settings/models" replace />} />
            <Route path="/settings/:tab" element={<Settings />} />
            <Route path="*" element={<Navigate to="/live" replace />} />
          </Routes>
        </main>
      </div>
    </HashRouter>
  );
}
```

지워지는 것: `useState`, `KEY`, `collapsed`/`toggle`, `localStorage` 접근.

- [ ] **Step 3: Sidebar 삭제, 로케일 키 삭제**

Run: `git rm src/components/Sidebar.tsx`

세 로케일의 `"nav"` 줄에서 `"collapse": "…", "expand": "…", ` 를 지운다. 결과:

```json
"nav": { "live": "라이브", "history": "히스토리", "settings": "설정", "search": "히스토리 검색", "about": "정보" },
```
```json
"nav": { "live": "Live", "history": "History", "settings": "Settings", "search": "Search history", "about": "About" },
```
```json
"nav": { "live": "ライブ", "history": "履歴", "settings": "設定", "search": "履歴を検索", "about": "情報" },
```

- [ ] **Step 4: 검증**

Run: `grep -rn "Sidebar\|nav.collapse\|nav.expand\|wide:" src; yarn test && yarn build`
Expected: grep 결과 없음. 테스트·빌드 PASS.

Run: `yarn tauri dev` 로 앱을 띄워 확인(한 번에 끝낸다):
- 상단 바에 로고 · 라이브/히스토리/설정 탭 · 검색 · 시작 버튼이 한 줄로 보인다.
- 활성 탭이 연보라 바탕 + 퍼플 글자.
- 시작 → 버튼이 표면색으로 바뀌며 무지개 링이 돌고 퍼플 파형이 뛴다. 정지 → 퍼플 "시작" 으로 복귀.
- 검색에 단어 입력 후 Enter → 히스토리로 이동하고 검색어가 적용된다.
- 설정 › 일반 맨 아래 "정보" 버튼 → 대화상자에 아이콘·버전·칩·메모리.

- [ ] **Step 5: 커밋**

```bash
git add -A src/components src/pages/MainApp.tsx src/locales
git commit -m "feat(ui): 사이드바를 상단 바로 교체

로고 · 탭 · 검색 · 시작/정지를 상단 바 한 줄에 둔다. 접기/펼치기, wide
브레이크포인트, localStorage 키 삭제. 시작 버튼의 무지개 링·파형은 그대로.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01U9PLCfTJosJ5x85Rk936CS"
```

---

### Task 4: 라이브 컨트롤 줄

**Files:**
- Modify: `src/pages/main/Live.tsx`
- Modify: `src/locales/ko.json`, `en.json`, `ja.json` (`live.overlayOn`, `live.overlayOff` 삭제)

**Interfaces:**
- Consumes: `useSettings()` → `{ settings, update }` (`update` 는 부분 객체를 받는다: `update({ asr: { source_lang } })`, `update({ overlay: { subtitle_lang } })`, `update({ overlay: { enabled } })`). `SessionBadges({ src, tgt, asrModel, translator })` 기존 컴포넌트. 타입 `SourceLang`, `UiLang` 은 `src/lib/types.ts` 에 있다.

- [ ] **Step 1: `Live.tsx` 전체 교체**

```tsx
import { useEffect, useRef } from "react";
import { useTranslation } from "react-i18next";
import { SessionBadges } from "../../components/SessionBadges";
import { translatorLabel, useModels } from "../../lib/models";
import { clock, useSession } from "../../lib/session";
import { useSettings } from "../../lib/settings";
import type { SourceLang, UiLang } from "../../lib/types";

export default function Live() {
  const { t } = useTranslation();
  const { view } = useSession();
  const { settings, update } = useSettings();
  const models = useModels((s) => s.models);
  const end = useRef<HTMLDivElement>(null);

  // 새 줄이 들어오면 바닥에 붙인다. 안 그러면 말이 화면 밖에서 흐른다.
  useEffect(() => { end.current?.scrollIntoView({ block: "end" }); }, [view.finals[view.finals.length - 1]?.id, view.partial?.text]);

  if (!settings) return null;
  const name = (id: string) => (id ? models.find((m) => m.info.id === id)?.info.name ?? id : "—");
  // 원문만 모드는 번역이 없으니 타겟 select 와 번역 모델 배지를 뺀다.
  const translating = settings.overlay.display_mode !== "source";
  const tr = settings.translation;
  const translator = translating ? translatorLabel(tr.backend === "local" ? `local:${tr.local_model}` : `cloud:${tr.cloud.provider}/${tr.cloud.model}`, name) : null;
  const badge = "badge badge-ghost badge-sm";

  return (
    <div className="flex h-full flex-col gap-3">
      {/* 컨트롤 줄: 언어 쌍(설정값, 다음 세션부터 적용) · 상태 점 · 모델 배지 · 오버레이 토글 */}
      <div className="flex flex-wrap items-center gap-2 text-xs text-fg-muted">
        <select className="select select-sm w-32" aria-label={t("translation.sourceLang")} value={settings.asr.source_lang} onChange={(e) => update({ asr: { source_lang: e.target.value as SourceLang } })}>
          <option value="auto">{t("translation.auto")}</option><option value="ko">{t("general.langKo")}</option><option value="en">{t("general.langEn")}</option><option value="ja">{t("general.langJa")}</option>
        </select>
        {translating && (
          <>
            <span aria-hidden="true">→</span>
            <select className="select select-sm w-32" aria-label={t("translation.targetLang")} value={settings.overlay.subtitle_lang} onChange={(e) => update({ overlay: { subtitle_lang: e.target.value as UiLang } })}>
              <option value="system">{t("general.langSystem")}</option><option value="ko">{t("general.langKo")}</option><option value="en">{t("general.langEn")}</option><option value="ja">{t("general.langJa")}</option>
            </select>
          </>
        )}
        <span className={`ml-1 h-2 w-2 shrink-0 rounded-full ${view.capturing ? "bg-success" : "bg-fg-muted"}`} />
        {/* 캡처 중에는 돌고 있는 세션의 값(언어 포함)을, 정지 중에는 설정된 모델만 보여준다. */}
        {view.capturing
          ? <SessionBadges src={view.sourceLang ?? "auto"} tgt={translating ? view.targetLang : null} asrModel={view.modelId ?? settings.asr.model_id} translator={translator} />
          : <><span className={badge}>{name(settings.asr.model_id)}</span>{translator && <span className={badge}>{translator}</span>}</>}
        {view.gpuFallback && <span className="badge badge-warning badge-soft badge-sm">{t("live.cpuFallback")}</span>}
        {view.lagging && <span className="badge badge-warning badge-soft badge-sm">{t("live.lagging")}</span>}
        <label className="ml-auto flex items-center gap-2 text-sm text-base-content">
          {t("live.overlay")}
          <input type="checkbox" role="switch" className="toggle toggle-primary toggle-sm" checked={settings.overlay.enabled} onChange={(e) => update({ overlay: { enabled: e.target.checked } })} />
        </label>
      </div>

      <div className="flex-1 overflow-auto rounded-box border border-base-300 bg-base-100 p-4 shadow-kr">
        <div className="flex flex-col gap-2 text-sm">
          {view.finals.map((f) => (
            <div key={f.id} className="flex gap-3">
              <span className="shrink-0 tabular-nums text-fg-muted">{clock(f.start_ms)}</span>
              <div className="min-w-0 break-words">
                <div className="text-fg-muted">{f.text}</div>
                {f.tgt && <div className="font-semibold">{f.tgt}</div>}
              </div>
            </div>
          ))}
          {view.partial && (
            <div className="flex gap-3 text-fg-muted opacity-70">
              <span className="shrink-0 tabular-nums">{clock(view.partial.start_ms)}</span>
              <span className="min-w-0 break-words">{view.partial.text}</span>
            </div>
          )}
          <div ref={end} />
        </div>
      </div>
    </div>
  );
}
```

`view.sourceLang`, `view.targetLang`, `view.modelId` 는 `src/lib/session.ts` 의 view 필드(모두 `string | null`)로, 기존 `Live.tsx` 가 이미 쓰던 것이다.

- [ ] **Step 2: 로케일 키 삭제**

세 파일의 `"live"` 줄에서 `"overlayOn": "…", "overlayOff": "…", ` 를 지운다. 결과:

```json
"live": { "start": "시작", "stop": "정지", "overlay": "오버레이", "stopping": "정지 중…", "cpuFallback": "CPU", "lagging": "지연" },
```
```json
"live": { "start": "Start", "stop": "Stop", "overlay": "Overlay", "stopping": "Stopping…", "cpuFallback": "CPU", "lagging": "Lagging" },
```
```json
"live": { "start": "開始", "stop": "停止", "overlay": "オーバーレイ", "stopping": "停止中…", "cpuFallback": "CPU", "lagging": "遅延" },
```

- [ ] **Step 3: 검증**

Run: `grep -rn "overlayOn\|overlayOff" src; yarn test && yarn build`
Expected: grep 결과 없음. PASS.

`yarn tauri dev` 확인:
- 라이브 첫 줄에 원어 → 타겟 select, 상태 점, 모델 배지, 오른쪽 끝 오버레이 토글.
- 설정 › 오버레이에서 표시 모드를 "원문만" 으로 → 타겟 select 와 번역 배지가 사라진다.
- 원어 select 변경 → 설정 › 번역의 원어가 같이 바뀐다(같은 설정값).
- 토글 → 오버레이 창이 뜨고 사라진다.
- 캡처 시작 → 배지에 세션 언어 쌍이 나온다.

- [ ] **Step 4: 커밋**

```bash
git add src/pages/main/Live.tsx src/locales
git commit -m "feat(ui): 라이브 화면에 언어 쌍·오버레이 토글 컨트롤 줄

원어/타겟 select 와 오버레이 스위치를 자막 바로 위에 둔다. 제목 제거,
켬/끔 버튼 두 개를 토글 하나로. 자막 카드는 흰 표면 + 테두리 + shadow-kr.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01U9PLCfTJosJ5x85Rk936CS"
```

---

### Task 5: 설정 화면과 공용 컴포넌트 스타일

**Files:**
- Modify: `src/pages/Settings.tsx`
- Modify: `src/components/SettingGroup.tsx`
- Modify: `src/components/ModelPicker.tsx`
- Modify: `src/components/ModelRow.tsx`
- Modify: `src/components/PermissionRow.tsx`

**Interfaces:** 변경 없음(모두 클래스 문자열만).

- [ ] **Step 1: `Settings.tsx` 제목 제거**

`<h2 className="text-2xl font-bold">{t("nav.settings")}</h2>` 줄을 지운다. `useTranslation` 은 탭 라벨에 계속 쓰이므로 import 는 남긴다. 바깥 div 는 `flex h-full w-full max-w-3xl flex-col gap-4` 그대로.

- [ ] **Step 2: `SettingGroup.tsx`**

```tsx
export function SettingGroup({ children }: { children: ReactNode }) {
  return <div className="divide-y divide-base-300 rounded-box border border-base-300 bg-base-100 shadow-kr">{children}</div>;
}
```

`SettingRow` 는 그대로.

- [ ] **Step 3: `ModelPicker.tsx` 프리셋 카드**

프리셋 `<button>` 의 className 을 아래로:

```tsx
className={`flex flex-col items-start gap-1 rounded-box border border-base-300 bg-base-100 px-4 py-3 text-left text-sm shadow-kr hover:bg-base-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary ${on ? "border-primary ring-1 ring-inset ring-primary" : ""}`}
```

배지 네 개:

```tsx
{p.id === "balanced" && <span className="badge badge-secondary badge-sm">{t("models.badgeRecommended")}</span>}
{st.inUse && <span className="badge badge-success badge-soft badge-sm">{t("models.badgeInUse")}</span>}
{st.installed && !st.inUse && <span className="badge badge-ghost badge-sm">{t("models.badgeInstalled")}</span>}
{p.heavy && <span className="badge badge-warning badge-soft badge-sm">{t("models.badgeHeavy")}</span>}
```

- [ ] **Step 4: `ModelRow.tsx`**

행 `<div role="button">` 의 className:

```tsx
className={`grid grid-cols-[1fr_auto] items-center gap-3 rounded-box border border-base-300 bg-base-100 px-4 py-3 text-left text-sm shadow-kr hover:bg-base-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary ${selected ? "border-primary ring-1 ring-inset ring-primary" : ""}`}
```

배지:

```tsx
{status.in_use && status.installed && <span className="badge badge-success badge-soft badge-sm">{t("models.badgeInUse")}</span>}
{status.installed && !status.in_use && <span className="badge badge-ghost badge-sm">{t("models.badgeInstalled")}</span>}
{status.balanced && <span className="badge badge-secondary badge-sm">{t("models.badgeRecommended")}</span>}
{status.fit === "heavy" && <span className="badge badge-warning badge-soft badge-sm">{t("models.badgeHeavy")}</span>}
```

`Meter` 의 빈 칸 색 `bg-neutral` → `bg-base-300`(라이트에서 neutral 은 표면색과 같아 안 보인다):

```tsx
{[1, 2, 3, 4, 5].map((i) => <span key={i} className={`h-1.5 w-1.5 rounded-[2px] ${i <= value ? (accent ? "bg-primary" : "bg-fg-muted") : "bg-base-300"}`} />)}
```

- [ ] **Step 5: `PermissionRow.tsx`**

허용됨 아이콘 `bg-primary text-primary-content` → `bg-success text-success-content`. 버튼 두 개에서 `btn-neutral` → `btn`(기본):

```tsx
<button type="button" className={`btn btn-sm ${perm === "denied" ? "" : "btn-primary"}`} onClick={check}>{t("permission.check")}</button>
<button type="button" className={`btn btn-sm ${perm === "denied" ? "btn-primary" : "btn-outline"}`} onClick={() => api.openPrivacySettings().catch(showError)}>{t("permission.openSettings")}</button>
```

- [ ] **Step 6: 검증**

Run: `yarn test && yarn build`
Expected: PASS.

`yarn tauri dev` 확인(라이트·다크 각각):
- 설정 서브탭 밑줄이 퍼플. 그룹 카드가 흰 표면 + 테두리 + 은은한 그림자.
- 모델 탭: 선택된 프리셋 카드에 퍼플 테두리, 추천 배지 연보라, 사용 중 배지 연초록, 버거울 수 있음 앰버.
- 직접 고르기 펼침 → 모델 행도 같은 카드 스타일, 품질 미터 빈 칸이 보인다.
- 일반 탭(macOS): 권한 허용됨 아이콘이 초록.

- [ ] **Step 7: 커밋**

```bash
git add src/pages/Settings.tsx src/components/SettingGroup.tsx src/components/ModelPicker.tsx src/components/ModelRow.tsx src/components/PermissionRow.tsx
git commit -m "feat(ui): 설정·모델 카드·배지를 Kraken 스타일로

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01U9PLCfTJosJ5x85Rk936CS"
```

---

### Task 6: 히스토리

**Files:**
- Modify: `src/pages/main/History.tsx`

**Interfaces:** 변경 없음. `?q=` 처리, `api.history*` 호출, `SessionBadges` 그대로.

- [ ] **Step 1: 제목·검색 줄 제거**

return 맨 위의 아래 블록을 통째로 지운다(검색은 TopBar 가 맡는다. `q` 상태와 `useEffect` 들은 `?q=` 처리에 쓰이므로 남긴다):

```tsx
<div className="flex items-center justify-between gap-4">
  <h2 className="text-2xl font-bold">{t("nav.history")}</h2>
  <input … className="input input-sm w-56 rounded-full" />
</div>
```

- [ ] **Step 2: 세션 목록을 카드로**

마지막 분기(`sessions.map`)를 아래로:

```tsx
<div className="flex flex-col gap-0.5 rounded-box border border-base-300 bg-base-100 p-1 shadow-kr">
  {sessions.map((s) => (
    <button key={s.id} type="button" onClick={() => setSel(s.id)} className="flex flex-wrap items-center gap-2 rounded-lg px-3 py-2 text-left text-sm hover:bg-base-200">
      <span className="font-semibold">{when(s.started_at)}</span>
      <span className="text-xs text-fg-muted">{duration(s)} · {t("history.segments", { count: s.segments })}</span>
      <span className="ml-auto flex flex-wrap items-center gap-1 text-xs text-fg-muted">{badges(s)}</span>
    </button>
  ))}
</div>
```

- [ ] **Step 3: 검색 결과·상세·빈 상태**

검색 결과(`hits.map`) 바깥 div: `flex flex-col gap-1` → `flex flex-col gap-0.5 rounded-box border border-base-300 bg-base-100 p-1 shadow-kr`. 각 결과 버튼: `hover:bg-base-300` → `hover:bg-base-200`, `rounded-md` → `rounded-lg`.

상세 버튼 줄:

```tsx
<button type="button" className="btn btn-ghost btn-sm" onClick={() => setSel(null)}>{`← ${t("nav.history")}`}</button>
<div className="flex gap-2">
  <button type="button" className="btn btn-sm" onClick={() => exportAs(sel, "txt")}>{t("history.exportTxt")}</button>
  <button type="button" className="btn btn-sm" onClick={() => exportAs(sel, "srt")}>{t("history.exportSrt")}</button>
  <button type="button" className="btn btn-outline btn-error btn-sm" onClick={() => remove(sel)}>{t("history.delete")}</button>
</div>
```

상세 자막 카드: `flex flex-col gap-2 rounded-box bg-base-200 p-4 text-sm` → `flex flex-col gap-2 rounded-box border border-base-300 bg-base-100 p-4 text-sm shadow-kr`. 안의 원문 `<div>{s.src_text}</div>` → `<div className="text-fg-muted">{s.src_text}</div>`, 번역 `font-bold` → `font-semibold`.

빈 상태: `rounded-lg bg-base-200 p-4 text-sm text-fg-muted` → `rounded-box border border-base-300 bg-base-100 p-4 text-sm text-fg-muted shadow-kr`.

- [ ] **Step 4: 검증**

Run: `yarn test && yarn build`
Expected: PASS.

`yarn tauri dev` 확인: 히스토리 목록이 카드 안에 행으로, hover 시 표면색. 상단 바 검색 → 결과 카드. 세션 클릭 → 상세, 삭제 버튼이 빨간 outline.

- [ ] **Step 5: 커밋**

```bash
git add src/pages/main/History.tsx
git commit -m "feat(ui): 히스토리를 카드 목록으로, 검색은 상단 바로

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01U9PLCfTJosJ5x85Rk936CS"
```

---

### Task 7: 온보딩

**Files:**
- Modify: `src/pages/Onboarding.tsx`

**Interfaces:** 변경 없음.

- [ ] **Step 1: 클래스 교체**

언어 버튼: `btn-neutral` → 기본 `btn`.

```tsx
<button key={v} type="button" className={`btn btn-sm ${settings.general.ui_language === v ? "btn-primary" : ""}`} onClick={() => update({ general: { ui_language: v } })}>{text}</button>
```

완료 단계 체크 표시(`mark`): 설치됨 `bg-primary text-primary-content` → `bg-success text-success-content`. 나머지(대기 `bg-neutral`, 미설치 `bg-error`)는 그대로.

제목: `<h2 className="text-2xl font-bold">` → `<h2 className="text-2xl font-bold tracking-tight">`.

- [ ] **Step 2: 검증**

Run: `yarn test && yarn build`
Expected: PASS.

온보딩 창은 설정 파일의 `general.onboarding_done` 이 `false` 일 때 뜬다. 앱을 끈 상태에서 `~/Library/Application Support/org.bobpark.babelay/settings.json`(Windows 는 `%APPDATA%\org.bobpark.babelay\settings.json`)을 열어 `"onboarding_done": true` 를 `false` 로 바꾸고 `yarn tauri dev`. 확인 항목: 단계 표시 완료·현재가 퍼플, 언어 버튼 선택이 퍼플, 완료 단계 설치됨 체크가 초록.

- [ ] **Step 3: 커밋**

```bash
git add src/pages/Onboarding.tsx
git commit -m "feat(ui): 온보딩 버튼·체크 색을 Kraken 토큰에 맞춤

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01U9PLCfTJosJ5x85Rk936CS"
```

---

### Task 8: 앱 아이콘

**Files:**
- Modify: `assets/icon.svg`
- Regenerate: `src-tauri/icons/*` (`yarn icons`)

**Interfaces:** `assets/icon.svg` 는 `TopBar.tsx` 와 `AboutDialog.tsx` 가 import 한다. 파일명·viewBox 유지.

- [ ] **Step 1: `assets/icon.svg` 전체 교체**

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="1024" height="1024" viewBox="0 0 1024 1024">
  <defs>
    <linearGradient id="bg" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0" stop-color="#8b5cf6"/>
      <stop offset="1" stop-color="#5b1ecf"/>
    </linearGradient>
  </defs>
  <rect x="100" y="100" width="824" height="824" rx="185" fill="url(#bg)"/>
  <rect x="384" y="282" width="256" height="76" rx="38" fill="#ffffff"/>
  <rect x="332" y="410" width="360" height="76" rx="38" fill="#ffffff" opacity="0.85"/>
  <rect x="280" y="538" width="464" height="76" rx="38" fill="#ffffff" opacity="0.6"/>
  <rect x="228" y="666" width="568" height="76" rx="38" fill="#ffffff" opacity="0.35"/>
</svg>
```

- [ ] **Step 2: 아이콘 재생성**

Run: `yarn icons`
Expected: `src-tauri/icons/` 의 png/icns/ico 가 갱신되고 "tray icons written" 출력. `git status` 에 `src-tauri/icons/*.png`, `icon.icns`, `icon.ico` 변경이 잡힌다(트레이 `tray-*.png` 는 `tray.svg` 기반이라 내용이 같아 변경 없음).

- [ ] **Step 3: 검증**

Run: `yarn build`
Expected: PASS.

`yarn tauri dev` 확인: 도크 아이콘이 퍼플 그라데이션 + 흰 줄. 상단 바 로고와 정보 창 아이콘도 같은 모양.

- [ ] **Step 4: 커밋**

```bash
git add assets/icon.svg src-tauri/icons
git commit -m "feat(icon): 앱 아이콘을 Kraken 퍼플로

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01U9PLCfTJosJ5x85Rk936CS"
```

---

### Task 9: 마무리 검증

**Files:** 없음(수정 없이 확인만). 문제가 나오면 해당 태스크 파일을 고치고 `fix(ui):` 로 커밋한다.

- [ ] **Step 1: 자동 검증**

Run: `yarn test && yarn build && grep -rn "rounded-full" src/components src/pages`
Expected: 테스트·빌드 PASS. `rounded-full` 은 상태 점(`h-2 w-2`, `h-1.5 w-1.5`)과 `PermissionIcon`/온보딩 `mark` 의 `h-6 w-6` 원, `btn-circle` 외에는 남지 않는다.

- [ ] **Step 2: 라이트·다크 화면 점검**

`yarn tauri dev` 실행 후 설정 › 일반 › 테마를 라이트, 다크 순서로 바꿔 가며 각각:
- 라이브: 정지 상태, 캡처 중(무지개 링·파형), 오버레이 토글 켬/끔.
- 히스토리: 목록, 상세, 검색 결과, 빈 상태(세션이 없을 때).
- 설정 4탭 전부. 번역 탭에서 클라우드 선택 시 API 키 입력·저장됨 배지.
- 모델 다운로드 토스트(진행 바가 퍼플), 업데이트 확인 토스트.
- 오버레이 위치 조정 모드: 링·핸들·안내 배지가 퍼플.

- [ ] **Step 3: 최종 확인 후 보고**

발견한 문제와 고친 커밋을 사용자에게 보고한다. 브랜치 병합은 `superpowers:finishing-a-development-branch` 를 따른다.
